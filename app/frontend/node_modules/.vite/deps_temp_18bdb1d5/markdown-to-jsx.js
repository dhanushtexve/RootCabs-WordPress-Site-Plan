import {
  require_react
} from "./chunk-QI6TWUTQ.js";
import {
  __toESM
} from "./chunk-ILHRZGIS.js";

// node_modules/markdown-to-jsx/dist/index.js
var c0 = __toESM(require_react());

// node_modules/markdown-to-jsx/dist/entities.browser.js
var j;
function B(z) {
  if (!j) if (typeof document < "u") j = document.createElement("textarea");
  else return;
  var q = "&" + z + ";";
  j.innerHTML = q;
  var v = j.value;
  return v !== q ? v : void 0;
}

// node_modules/markdown-to-jsx/dist/index.js
var N = 32;
var b = 9;
var N5 = 13;
var f = 10;
var e = 96;
var N0 = 126;
var z0 = 91;
var F5 = 94;
var o = 62;
var G5 = 35;
var J9 = 37;
var d = 45;
var V5 = 61;
var b0 = 92;
var A0 = 42;
var q0 = 95;
var Q0 = 60;
var b9 = 64;
var K5 = 93;
var p0 = 33;
var F9 = 38;
var g0 = 58;
var U9 = 70;
var P5 = 102;
var q9 = 104;
var S5 = 119;
var p5 = 116;
var y9 = 112;
var c9 = 115;
var N$ = 160;
var P$ = 12;
var w$ = 44;
var W9 = 59;
var s5 = 63;
var s0 = 46;
var H0 = 47;
var E5 = 39;
var w5 = 34;
var b5 = 43;
var S0 = 124;
var y5 = 123;
var R9 = 125;
var U5 = 40;
var q5 = 41;
var b$ = 78;
var y$ = 110;
var R$ = 79;
var L$ = 111;
var l9 = 120;
var o9 = 88;
var L9 = 30;
var r = 48;
var i = 57;
var p = 65;
var c = 90;
var G0 = 97;
var U0 = 122;
var t5 = 128;
var X5 = 32;
var a4 = { blockQuote: 0, breakLine: 1, breakThematic: 2, codeBlock: 3, codeInline: 4, footnote: 5, footnoteReference: 6, frontmatter: 7, gfmTask: 8, heading: 9, htmlBlock: 10, htmlComment: 11, htmlSelfClosing: 12, image: 13, link: 14, orderedList: 15, paragraph: 16, ref: 17, refCollection: 18, table: 19, text: 20, textFormatted: 21, unorderedList: 22 };
var _ = a4;
function g$($) {
  if (!S$($, "---")) return null;
  let Q = 3;
  for (; Q < $.length && ($[Q] === " " || $[Q] === "	"); ) Q++;
  if (Q < $.length && $[Q] === "\r" && Q++, Q >= $.length || $[Q] !== `
`) return null;
  Q++;
  let Z = false;
  for (; Q < $.length; ) {
    let Y = Q;
    for (; Q < $.length && $[Q] !== `
` && $[Q] !== "\r"; ) Q++;
    if (Q >= $.length) break;
    let G = Q;
    if ($[Q] === "\r" && Q++, Q < $.length && $[Q] === `
` && Q++, S$($, "---", Y)) return { endPos: Q, hasValidYaml: Z };
    if (!Z) {
      let V = Z6($, Y, G);
      if (V < G) {
        let K = $.charCodeAt(V);
        if (K >= G0 && K <= U0 || K >= p && K <= c || K >= r && K <= i || K === q0) {
          for (V++; V < G && (K = $.charCodeAt(V), K >= G0 && K <= U0 || K >= p && K <= c || K >= r && K <= i || K === q0 || K === d || K === s0); ) V++;
          V < G && $.charCodeAt(V) === g0 && (V++, V >= G ? Z = true : (K = $.charCodeAt(V), (K === N || K === b) && (Z = true)));
        }
      }
    }
  }
  return null;
}
var s4 = /&([a-zA-Z0-9]+|#[0-9]{1,7}|#x[0-9a-fA-F]{1,6});/gi;
var t4 = { class: "className", for: "htmlFor", allowfullscreen: "allowFullScreen", allowtransparency: "allowTransparency", autocomplete: "autoComplete", autofocus: "autoFocus", autoplay: "autoPlay", cellpadding: "cellPadding", cellspacing: "cellSpacing", charset: "charSet", classid: "classId", colspan: "colSpan", contenteditable: "contentEditable", contextmenu: "contextMenu", crossorigin: "crossOrigin", enctype: "encType", formaction: "formAction", formenctype: "formEncType", formmethod: "formMethod", formnovalidate: "formNoValidate", formtarget: "formTarget", frameborder: "frameBorder", hreflang: "hrefLang", inputmode: "inputMode", keyparams: "keyParams", keytype: "keyType", marginheight: "marginHeight", marginwidth: "marginWidth", maxlength: "maxLength", mediagroup: "mediaGroup", minlength: "minLength", novalidate: "noValidate", radiogroup: "radioGroup", readonly: "readOnly", rowspan: "rowSpan", spellcheck: "spellCheck", srcdoc: "srcDoc", srclang: "srcLang", srcset: "srcSet", tabindex: "tabIndex", usemap: "useMap", viewbox: "viewBox" };
var e4 = {};
function i9($) {
  if (!$) return e4;
  var Q = {};
  for (var Z in $) {
    var Y = Z.toLowerCase(), G = t4[Y];
    if (G) Q[G] = $[Z];
    else {
      var V = Z.indexOf(":");
      V !== -1 ? Q[Z.slice(0, V) + Z[V + 1].toUpperCase() + Z.slice(V + 2)] = $[Z] : Q[Z] = $[Z];
    }
  }
  return Q;
}
var f$ = /(\n|^[-*]\s|^#|^ {2,}|^-{2,}|^>\s|^<(div|p|h[1-6]|ul|ol|li|blockquote|pre|table|thead|tbody|tr|td|th|dl|dt|dd|hr|address|article|aside|details|dialog|figure|figcaption|footer|form|header|main|menu|nav|section|summary|textarea|fieldset|legend|center|dir|hgroup|marquee|search|output|template)\b)/i;
function o5($) {
  return $.indexOf("&") === -1 ? $ : $.replace(s4, (Q, Z) => {
    var Y = B(Z);
    if (Y) return Y;
    if (Z[0] === "#") {
      var G = Z[1] === "x" || Z[1] === "X" ? parseInt(Z.slice(2), 16) : parseInt(Z.slice(1), 10);
      return G === 0 || G >= 55296 && G <= 57343 || G > 1114111 ? "�" : G <= 65535 ? String.fromCharCode(G) : String.fromCharCode(55296 + (G - 65536 >> 10), 56320 + (G - 65536 & 1023));
    }
    return Q;
  });
}
var k$ = /(javascript|vbscript|data(?!:image)):/i;
function j5($) {
  if (k$.test($)) return null;
  if ($.indexOf("%") === -1) return $;
  try {
    let Q = decodeURIComponent($).replace(/[^A-Za-z0-9/:]/g, "");
    if (k$.test(Q)) return null;
  } catch {
    return null;
  }
  return $;
}
var d0 = {};
var i0;
var E0;
for (i0 = [192, 193, 194, 195, 196, 197, 224, 225, 226, 227, 228, 229, 230, 198], E0 = 0; E0 < i0.length; E0++) d0[i0[E0]] = "a";
for (d0[231] = d0[199] = "c", d0[240] = d0[208] = "d", i0 = [200, 201, 202, 203, 233, 232, 234, 235], E0 = 0; E0 < i0.length; E0++) d0[i0[E0]] = "e";
for (i0 = [207, 239, 206, 238, 205, 237, 204, 236], E0 = 0; E0 < i0.length; E0++) d0[i0[E0]] = "i";
for (d0[209] = d0[241] = "n", i0 = [248, 216, 339, 338, 213, 245, 212, 244, 211, 243, 210, 242], E0 = 0; E0 < i0.length; E0++) d0[i0[E0]] = "o";
for (i0 = [220, 252, 219, 251, 218, 250, 217, 249], E0 = 0; E0 < i0.length; E0++) d0[i0[E0]] = "u";
d0[376] = d0[255] = d0[221] = d0[253] = "y";
function f0($) {
  for (var Q = "", Z = -1, Y = $.length, G = 0; G < Y; G++) {
    var V = $.charCodeAt(G);
    if (V >= G0 && V <= U0 || V >= r && V <= i) {
      Z < 0 && (Z = G);
      continue;
    }
    if (V >= p && V <= c) {
      Z >= 0 && (Q += $.slice(Z, G), Z = -1), Q += String.fromCharCode(V + X5);
      continue;
    }
    if (V === N || V === d) {
      Z >= 0 && (Q += $.slice(Z, G), Z = -1), Q += "-";
      continue;
    }
    Z >= 0 && (Q += $.slice(Z, G), Z = -1);
    var K = d0[V];
    K && (Q += K);
  }
  return Z >= 0 && (Q += $.slice(Z)), Q;
}
function S$($, Q, Z) {
  return $.startsWith(Q, Z);
}
var E$ = /* @__PURE__ */ new Set(["area", "base", "br", "col", "embed", "hr", "img", "input", "link", "meta", "param", "source", "track", "wbr", "circle", "ellipse", "line", "path", "polygon", "polyline", "rect", "use", "stop", "animate", "set"]);
function r5($) {
  let Q = $.toLowerCase();
  if (E$.has(Q)) return true;
  let Z = Q.indexOf(":");
  return Z !== -1 ? (Q = Q.slice(Z + 1), E$.has(Q)) : false;
}
var T5 = 1;
var T$ = 2;
var c5 = 4;
var x$ = 8;
var $6 = 16;
var W5 = 32;
var R5 = 64;
var C$ = function() {
  var $ = new Uint8Array(128), Q;
  for ($[b] = T5, $[f] = T5 | T$, $[P$] = T5, $[N5] = T5 | T$, $[N] = T5, Q = p0; Q <= H0; Q++) $[Q] = c5;
  for (Q = g0; Q <= b9; Q++) $[Q] = c5;
  for (Q = z0; Q <= e; Q++) $[Q] = c5;
  for (Q = y5; Q <= N0; Q++) $[Q] = c5;
  for (Q = r; Q <= i; Q++) $[Q] = $6;
  for (Q = p; Q <= c; Q++) $[Q] = x$;
  for (Q = G0; Q <= U0; Q++) $[Q] = x$;
  return $;
}();
var m$ = /[\p{P}\p{S}]/u;
var h$ = new RegExp("\\p{Zs}", "u");
var d5 = [];
function u$($) {
  var Q = $.indexOf("\r"), Z = $.indexOf("\0");
  if (Q === -1 && Z === -1) return $;
  var Y = $.length;
  d5.length = 0;
  var G = 0, V = 0;
  for (Q === -1 ? V = Z : Z === -1 ? V = Q : V = Q < Z ? Q : Z; V < Y; V++) {
    var K = $.charCodeAt(V);
    K === N5 ? (G < V && d5.push($.slice(G, V)), V + 1 < Y && $.charCodeAt(V + 1) === f && V++, d5.push(`
`), G = V + 1) : K === 0 && (G < V && d5.push($.slice(G, V)), d5.push("�"), G = V + 1);
  }
  return G < Y && d5.push($.slice(G)), d5.join("");
}
function Z6($, Q, Z) {
  let Y = Z ?? $.length;
  for (; Q < Y && ($[Q] === " " || $[Q] === "	"); ) Q++;
  return Q;
}
function S9($) {
  if (!$) return false;
  for (var Q in $) return true;
  return false;
}
function p$($) {
  return { attrs: {}, children: [{ type: _.text, text: $ }], d: true, type: _.htmlBlock, tag: "header" };
}
var Q6 = /^\n+/;
function d$($) {
  for (var Q = $.length; Q > 0 && ($[Q - 1] === `
` || $[Q - 1] === "\r"); ) Q--;
  return $.slice(0, Q).replace(Q6, "") + `

`;
}
function l5($) {
  var _a, _b;
  if (($.type === _.htmlSelfClosing || $.type === _.htmlBlock) && $.b) return [];
  if ($.type === _.paragraph) {
    var Q = $.children;
    return Q ? Q.flatMap(l5) : [];
  }
  return $.type === _.text ? ((_a = $.text) == null ? void 0 : _a.trim()) ? [$] : [] : $.type === _.htmlBlock && $.children ? [{ ...$, children: (_b = $.children) == null ? void 0 : _b.flatMap(l5) }] : [$];
}
function a9($) {
  for (var Q = 0; Q < $.length; Q++) {
    if ($[Q].type === _.htmlBlock) {
      var Z = $[Q], Y = false;
      if (Z.d && Z.a && Q === $.length - 1) {
        var G = "</" + String(Z.tag).toLowerCase() + ">", V = Z.a.toLowerCase().indexOf(G);
        if (V !== -1) {
          var K = Z.a.slice(V + G.length).replace(/<\/[a-z][a-z0-9-]*\s*>/gi, "");
          K.trim() && (Y = true);
        }
      }
      Y || (Z.d = false);
    }
    "children" in $[Q] && $[Q].children && a9($[Q].children);
  }
}
function c$($, Q) {
  for (var Z = 0; Z < $.length; Z++) {
    var Y = $[Z];
    if (Y.type === _.paragraph && Y.children) for (var G = Y.children, V = 0; V < G.length; V++) {
      var K = G[V];
      if (K.type === _.htmlSelfClosing && K.b && K.tag.toLowerCase() === Q) {
        var X = $.slice(0, Z);
        V > 0 && X.push({ type: _.paragraph, children: G.slice(0, V) });
        var J = [];
        if (V + 1 < G.length) {
          var F = G.slice(V + 1).filter(function(U) {
            return !(U.type === _.htmlSelfClosing && U.b);
          });
          F.length > 0 && (J = F);
        }
        return J = J.concat($.slice(Z + 1)), { found: true, beforeClose: X, afterClose: J };
      }
    }
    if ((Y.type === _.htmlSelfClosing || Y.type === _.htmlBlock) && Y.b && Y.tag.toLowerCase() === Q) return { found: true, beforeClose: $.slice(0, Z), afterClose: $.slice(Z + 1) };
  }
  return { found: false, beforeClose: $, afterClose: [] };
}
function l$($, Q, Z) {
  var Y = $.replace(new RegExp("\\s*</" + Q + ">\\s*$", "i"), "");
  return Z ? t9(Y) : Y;
}
function n9($) {
  var Q = "";
  for (var Z in $) {
    var Y = $[Z];
    Y === true ? Q += " " + Z : Y != null && Y !== false && (Q += " " + Z + '="' + String(Y) + '"');
  }
  return Q;
}
function o$($) {
  var Q = [];
  if (!$) return Q;
  for (var Z in $) Z.charCodeAt(0) === 94 && Q.push({ identifier: Z, footnote: $[Z].target });
  return Q;
}
function e5($, Q, Z) {
  if (Q.indexOf(".") === -1) return ($ == null ? void 0 : $[Q]) || Z;
  for (var Y = $, G = Q.split("."), V = 0; V < G.length && (Y = Y == null ? void 0 : Y[G[V]], Y !== void 0); ) V++;
  return Y || Z;
}
function r$($) {
  for (var Q = false, Z = 0; Z < $.length; Z++) {
    var Y = $.charCodeAt(Z);
    if (Y <= N || Y === w5 || Y === J9 || Y === Q0 || Y === o || Y === z0 || Y === b0 || Y === K5 || Y === F5 || Y === e || Y >= 123) {
      Q = true;
      break;
    }
  }
  if (!Q) return $;
  for (var G = "", Z = 0; Z < $.length; Z++) {
    var Y = $.charCodeAt(Z);
    if (Y === J9 && Z + 2 < $.length) {
      var V = $.charCodeAt(Z + 1), K = $.charCodeAt(Z + 2);
      if ((V >= r && V <= i || V >= p && V <= U9 || V >= G0 && V <= P5) && (K >= r && K <= i || K >= p && K <= U9 || K >= G0 && K <= P5)) {
        G += $[Z] + $[Z + 1] + $[Z + 2], Z += 2;
        continue;
      }
    }
    if (Y >= 55296 && Y <= 57343) {
      if (Y <= 56319 && Z + 1 < $.length) {
        var X = $.charCodeAt(Z + 1);
        if (X >= 56320 && X <= 57343) {
          G += encodeURI($[Z] + $[Z + 1]), Z++;
          continue;
        }
      }
      G += $[Z];
      continue;
    }
    G += encodeURI($[Z]);
  }
  return G;
}
function i$(...$) {
  return $.filter(Boolean).join(" ");
}
var Y6 = /* @__PURE__ */ new Set(["title", "textarea", "style", "xmp", "iframe", "noembed", "noframes", "script", "plaintext"]);
var k9 = /<(\/?)(title|textarea|style|xmp|iframe|noembed|noframes|script|plaintext)(\s|>|\/)/gi;
function s9($) {
  return Y6.has($.toLowerCase());
}
function a$($) {
  return k9.lastIndex = 0, k9.test($);
}
function t9($) {
  return k9.lastIndex = 0, $.replace(k9, function(Q, Z, Y, G) {
    return "&lt;" + Z + Y + G;
  });
}
var O4 = /^<([a-zA-Z][a-zA-Z0-9-]*)\s[^>]*>/;
var H4 = /^<[A-Z]/;
var $$ = ["script", "pre", "style", "textarea"];
var B9 = new Set($$);
var G6 = /<(?:pre|script|style|textarea)\b/i;
var s$ = /<(?:pre|script|style|textarea)\b/iy;
var e9 = /^(\s{0,3}#[#\s]|\s{0,3}[-*+]\s|\s{0,3}\d+\.\s|\s{0,3}>\s|\s{0,3}```)/m;
var t$ = /^<([a-z][^ >/\n\r]*) ?([^>]*?)>/im;
var B4 = new Uint8Array(128);
(function() {
  for (var $ = [e, A0, q0, N0, V5, z0, p0, Q0, b0, F9, f, q9, S5, P5], Q = 0; Q < $.length; Q++) B4[$[Q]] = 1;
})();
var e$ = /([a-zA-Z_][a-zA-Z0-9_-]*)=(?:"([^"]*)"|'([^']*)')/g;
function D4($) {
  return B9.has($);
}
function A4($) {
  return G6.test($);
}
function j4($, Q, Z) {
  for (var Y = Q, G = Z; Y < G && ($.charCodeAt(Y) === N || $.charCodeAt(Y) === b); ) Y++;
  if (Y >= G) return false;
  $.charCodeAt(Y) === S0 && Y++;
  for (var V = 0; Y < G; ) {
    for (; Y < G && ($.charCodeAt(Y) === N || $.charCodeAt(Y) === b); ) Y++;
    if (Y >= G) break;
    if ($.charCodeAt(Y) === S0 && V > 0) {
      for (var K = Y + 1; K < G && ($.charCodeAt(K) === N || $.charCodeAt(K) === b); ) K++;
      if (K >= G) return true;
    }
    if ($.charCodeAt(Y) === g0 && Y++, Y >= G || $.charCodeAt(Y) !== d) return false;
    for (; Y < G && $.charCodeAt(Y) === d; ) Y++;
    for (Y < G && $.charCodeAt(Y) === g0 && Y++, V++; Y < G && ($.charCodeAt(Y) === N || $.charCodeAt(Y) === b); ) Y++;
    if (Y < G) if ($.charCodeAt(Y) === S0) Y++;
    else return false;
  }
  return V > 0;
}
var V6 = /[\u0000-\u001F\u007F]/g;
var K6 = { action: 1, background: 1, cite: 1, data: 1, formaction: 1, href: 1, longdesc: 1, poster: 1, src: 1, "xlink:href": 1 };
var X6 = /&#(x[0-9a-f]+|[0-9]+);?/gi;
function J6($) {
  return $.indexOf("&#") === -1 ? $ : $.replace(X6, function(Q, Z) {
    var Y = Z.charCodeAt(0) === l9 || Z.charCodeAt(0) === o9 ? parseInt(Z.slice(1), 16) : parseInt(Z, 10);
    return Y > 0 && Y <= 1114111 ? String.fromCodePoint(Y) : Q;
  });
}
function F6($, Q, Z) {
  if (Z && Q.charCodeAt(0) === y5) return false;
  var Y = $.charCodeAt(0), G = $.charCodeAt(1);
  if ((Y === L$ || Y === R$) && (G === y$ || G === b$)) return !(Z && Q === "");
  var V = $.toLowerCase();
  if (V === "srcdoc") return true;
  if (K6[V] === 1) {
    var K = J6(o5(Q));
    if (j5(K) === null) return true;
    var X = K.replace(V6, "");
    return X !== K && j5(X) === null;
  }
  return false;
}
function Z$($) {
  return "<" + ($.isClosing ? "/" : "") + $.tag + $.whitespaceBeforeAttrs + $.rawAttrs + ($.selfClosing ? $.hasSpaceBeforeSlash ? " />" : "/>" : ">");
}
function $4($, Q, Z, Y) {
  for (var G = [], V = Q, K = 0; K < Y.length; K += 2) {
    var X = Y[K];
    if (X > V) {
      var J = $.slice(V, X).trim();
      J && G.push(J);
    }
    V = Y[K + 1];
  }
  if (V < Z) {
    var F = $.slice(V, Z).trim();
    F && G.push(F);
  }
  return G.join(" ");
}
function M5($, Q) {
  if ($.charCodeAt(Q) !== Q0) return null;
  let Z = Q + 1, Y = $.length, G = false;
  $.charCodeAt(Z) === H0 && (Z++, G = true);
  let V = Z, K = $.charCodeAt(Z);
  if (!(K >= G0 && K <= U0 || K >= p && K <= c)) return null;
  for (; Z < Y && ($.charCodeAt(Z) >= G0 && $.charCodeAt(Z) <= U0 || $.charCodeAt(Z) >= p && $.charCodeAt(Z) <= c || $.charCodeAt(Z) >= r && $.charCodeAt(Z) <= i || $.charCodeAt(Z) === d); ) Z++;
  let X = $.slice(V, Z);
  if (!X) return null;
  let J = X.charCodeAt(0), F = J >= p && J <= c || X.indexOf("-") !== -1, U = Z;
  for (; Z < Y && ($.charCodeAt(Z) === N || $.charCodeAt(Z) === b || $.charCodeAt(Z) === f); ) Z++;
  let W = $.slice(U, Z);
  if (Z === U && Z < Y) {
    var O = $.charCodeAt(Z);
    if (O !== o && O !== H0) return null;
  }
  let z = Z, B2 = {}, A = false;
  for (var q = null; Z < Y; ) {
    let u = $.charCodeAt(Z);
    if (u === o) {
      let E = q ? $4($, z, Z, q) : $.slice(z, Z);
      return { tag: X, attrs: B2, selfClosing: false, end: Z + 1, rawAttrs: E, whitespaceBeforeAttrs: W, isClosing: G, hasSpaceBeforeSlash: A, sanitized: q !== null };
    }
    if (u === N || u === b || u === f) {
      Z++;
      continue;
    }
    if (u === H0 && Z + 1 < Y && $.charCodeAt(Z + 1) === o) {
      A = Z > z && $.charCodeAt(Z - 1) === N;
      let E = q ? $4($, z, Z, q) : $.slice(z, Z);
      return { tag: X, attrs: B2, selfClosing: true, end: Z + 2, rawAttrs: E, whitespaceBeforeAttrs: W, isClosing: G, hasSpaceBeforeSlash: A, sanitized: q !== null };
    }
    var H = Z, j2 = $.charCodeAt(Z);
    if (!(j2 >= G0 && j2 <= U0 || j2 >= p && j2 <= c || j2 === q0 || j2 === g0)) return null;
    for (Z++; Z < Y; ) {
      var M = $.charCodeAt(Z);
      if (M >= G0 && M <= U0 || M >= p && M <= c || M >= r && M <= i || M === q0 || M === s0 || M === g0 || M === d) Z++;
      else break;
    }
    for (var y = $.slice(H, Z), v = Z; Z < Y && ($.charCodeAt(Z) === N || $.charCodeAt(Z) === b); ) Z++;
    var D, w;
    if ($.charCodeAt(Z) !== V5) D = "", w = v;
    else {
      for (Z++; Z < Y && ($.charCodeAt(Z) === N || $.charCodeAt(Z) === b); ) Z++;
      var L = $.charCodeAt(Z);
      if (L === w5 || L === E5) {
        Z++;
        for (var R = Z; Z < Y && $.charCodeAt(Z) !== L; ) Z++;
        if (Z >= Y) return null;
        if (D = $.slice(R, Z), Z++, Z < Y) {
          var I = $.charCodeAt(Z);
          if (I !== N && I !== b && I !== f && I !== o && I !== H0) return null;
        }
        w = Z;
      } else if (L === y5) {
        var x = 1, R = Z;
        for (Z++; Z < Y && x > 0; ) {
          var M = $.charCodeAt(Z);
          M === y5 ? x++ : M === R9 && x--, Z++;
        }
        D = $.slice(R, Z), w = Z;
      } else {
        for (var R = Z; Z < Y; ) {
          var k = $.charCodeAt(Z);
          if (k === N || k === b || k === o || k === f || k === w5 || k === E5 || k === V5 || k === Q0 || k === e) break;
          Z++;
        }
        if (Z === R) return null;
        D = $.slice(R, Z), w = Z;
      }
    }
    F6(y, D, F) ? (q === null && (q = []), q.push(H, w)) : B2[y] = D;
  }
  return null;
}
function g5($) {
  var Q = $.indexOf("<");
  if (Q === -1) return $;
  for (var Z = "", Y = 0, G = false; Q !== -1; ) {
    var V = M5($, Q);
    V ? (V.sanitized && (Z += $.slice(Y, Q) + Z$(V), Y = V.end, G = true), Q = $.indexOf("<", V.end)) : Q = $.indexOf("<", Q + 1);
  }
  return G ? Z + $.slice(Y) : $;
}
function U6($, Q, Z) {
  if (!Z.optimizeForStreaming && $.indexOf("[") === -1) return false;
  for (var Y = 0, G = $.length, V = false, K = false; Y < G; ) {
    for (var X = $.indexOf(`
`, Y), J = X < 0 ? G : X, F = Y, U = 0; F < J && U < 4; ) if ($.charCodeAt(F) === N) U++, F++;
    else if ($.charCodeAt(F) === b) U += 4, F++;
    else break;
    if (F >= J) {
      V = false, Y = X < 0 ? G : X + 1;
      continue;
    }
    if (U < 4) {
      var W = $.charCodeAt(F);
      if (W === e || W === N0) {
        for (var O = W, z = 0, B2 = F; B2 < J && $.charCodeAt(B2) === O; ) z++, B2++;
        if (z >= 3) {
          var A = true;
          if (O === e) {
            for (var q = B2; q < J; q++) if ($.charCodeAt(q) === e) {
              A = false;
              break;
            }
          }
          if (A) {
            V = false;
            for (var H = X < 0 ? G : X + 1; H < G; ) {
              for (var j2 = H, M = 0; j2 < G && M < 4; ) {
                var y = $.charCodeAt(j2);
                if (y === N) M++, j2++;
                else if (y === b) M += 4, j2++;
                else break;
              }
              if (M < 4 && j2 < G && $.charCodeAt(j2) === O) {
                for (var v = 0; j2 < G && $.charCodeAt(j2) === O; ) v++, j2++;
                if (v >= z) {
                  for (; j2 < G && ($.charCodeAt(j2) === N || $.charCodeAt(j2) === b); ) j2++;
                  if (j2 >= G || $.charCodeAt(j2) === f) {
                    Y = j2 >= G ? G : j2 + 1;
                    break;
                  }
                }
              }
              for (; H < G && $.charCodeAt(H) !== f; ) H++;
              H < G && H++;
            }
            H >= G && (Y = G, K = true);
            continue;
          }
        }
      }
    }
    for (var D = F; D < J && $.charCodeAt(D) === o; ) {
      D++, D < J && $.charCodeAt(D) === N && D++;
      for (var w = 0; D < J && w < 4; ) if ($.charCodeAt(D) === N) w++, D++;
      else if ($.charCodeAt(D) === b) w += 4, D++;
      else break;
      if (w >= 4) break;
      V = false;
    }
    if (!V && U < 4 && D < J && $.charCodeAt(D) === z0 && !(D + 1 < G && $.charCodeAt(D + 1) === F5)) {
      var L = M4($, D, Q);
      if (L) {
        Y = L, V = false;
        continue;
      }
    }
    var R = $.charCodeAt(F);
    if (R === G5 && U < 4) V = false;
    else if (U < 4 && (R === d || R === A0 || R === q0)) {
      for (var I = F, x = 0; I < J; ) {
        var k = $.charCodeAt(I);
        if (k === R) x++;
        else if (k !== N && k !== b) break;
        I++;
      }
      V = !(x >= 3 && I >= J);
    } else V = true;
    Y = X < 0 ? G : X + 1;
  }
  return K;
}
function M4($, Q, Z) {
  let Y = $.length;
  if ($.charCodeAt(Q) !== z0) return null;
  let G = Q + 1 < Y && $.charCodeAt(Q + 1) === F5, V = Q + 1;
  for (; V < Y; ) {
    var K = $.charCodeAt(V);
    if (K === K5) {
      V++;
      break;
    }
    if (K === z0) return null;
    K === b0 && V + 1 < Y && V++, V++;
  }
  if (V > Y || $.charCodeAt(V - 1) !== K5) return null;
  let X = $.slice(Q + 1, V - 1);
  if (X.length > 999) return null;
  let J = E9(X);
  if (!J || V >= Y || $.charCodeAt(V) !== g0) return null;
  V++;
  let F = false;
  for (; V < Y; ) {
    let u = $.charCodeAt(V);
    if (u === N || u === b) V++;
    else if (u === f && !F) F = true, V++;
    else break;
  }
  if (G) {
    let u = $.indexOf(`
`, V), E = u < 0 ? Y : u, m = $.slice(V, E).trim();
    return Z[J] = { target: m, title: void 0 }, u < 0 ? Y : u + 1;
  }
  var U;
  if (V < Y && $.charCodeAt(V) === Q0) {
    V++;
    for (var W = V; V < Y && $.charCodeAt(V) !== o && $.charCodeAt(V) !== f; ) $.charCodeAt(V) === b0 && V + 1 < Y && V++, V++;
    if (V >= Y || $.charCodeAt(V) !== o) return null;
    U = $.slice(W, V), V++;
    for (var O = $.indexOf(`
`, V), z = O < 0 ? Y : O, B2 = V; B2 < z && ($.charCodeAt(B2) === N || $.charCodeAt(B2) === b); ) B2++;
    if (B2 < z) {
      if (B2 === V) return null;
      var A = $.charCodeAt(B2);
      if (A !== w5 && A !== E5 && A !== U5) return null;
    }
  } else {
    for (var W = V, q = 0; V < Y; ) {
      var K = $.charCodeAt(V);
      if (K === U5) q++;
      else if (K === q5) {
        if (q === 0) break;
        q--;
      } else {
        if (K === N || K === b || K === f) break;
        K === b0 && V + 1 < Y && V++;
      }
      V++;
    }
    if (U = $.slice(W, V), !U) return null;
  }
  for (; V < Y && ($.charCodeAt(V) === N || $.charCodeAt(V) === b); ) V++;
  var H = $.indexOf(`
`, V), j2 = H < 0 ? Y : H, M, y = false, v = V, D = V;
  if (V === j2 && V < Y) for (D = V + 1; D < Y && ($.charCodeAt(D) === N || $.charCodeAt(D) === b); ) D++;
  if (D < Y) {
    var w = $.charCodeAt(D);
    if (w === w5 || w === E5 || w === U5) {
      for (var L = w === U5 ? 41 : w, R = D + 1, I = R; R < Y; ) {
        var x = $.charCodeAt(R);
        if (x === L) {
          for (var k = R + 1; k < Y && ($.charCodeAt(k) === N || $.charCodeAt(k) === b); ) k++;
          (k >= Y || $.charCodeAt(k) === f) && (M = $.slice(I, R), y = true, v = k < Y ? k + 1 : Y);
          break;
        }
        if (x === b0 && R + 1 < Y) {
          R += 2;
          continue;
        }
        if (x === f && R + 1 < Y && $.charCodeAt(R + 1) === f) break;
        R++;
      }
      if (!y && D === V) return null;
    }
  }
  if (y) return Z[J] || (Z[J] = { target: Z9(U), title: M !== void 0 ? o5(Z9(M)) : M }), v;
  for (; V < j2 && ($.charCodeAt(V) === N || $.charCodeAt(V) === b); ) V++;
  return V < j2 ? null : (Z[J] || (Z[J] = { target: Z9(U), title: M }), H < 0 ? Y : H + 1);
}
var t0 = new Uint8Array(C$);
for (t0[G5] |= W5, t0[o] |= W5, t0[d] |= W5 | R5, t0[b5] |= W5, t0[A0] |= W5 | R5, t0[q0] |= W5 | R5, t0[e] |= W5 | R5, t0[N0] |= W5 | R5, t0[Q0] |= W5 | R5, t0[z0] |= R5, t0[p0] |= R5, t0[S0] |= W5, z9 = r; z9 <= i; z9++) t0[z9] |= W5;
var z9;
function Z4($) {
  if ($.indexOf("[") < 0 && $.indexOf("]") < 0) return false;
  for (var Q = 0; Q < $.length; Q++) {
    if ($.charCodeAt(Q) === b0) {
      Q++;
      continue;
    }
    if ($.charCodeAt(Q) === z0 || $.charCodeAt(Q) === K5) return true;
  }
  return false;
}
function E9($) {
  for (var Q = $.length, Z = true, Y = Q > 0, G = 0; G < Q; G++) {
    var V = $.charCodeAt(G);
    if (V === N) {
      if (Z) {
        Y = false;
        break;
      }
      Z = true;
    } else if (V < 33 || V > 126 || V >= p && V <= c) {
      Y = false;
      break;
    } else Z = false;
  }
  if (Y && !Z) return $;
  var K = $.replace(/\s+/g, " ").trim();
  return K.indexOf("ẞ") !== -1 ? K.replace(/\u1E9E/g, "ss").toLowerCase() : K.toLowerCase();
}
function X$($) {
  return $ < t5 ? t0[$] : $ === N$ ? T5 : 0;
}
function Z9($) {
  return $.indexOf("\\") === -1 ? $ : $.replace(/\\([!"#$%&'()*+,\-./:;<=>?@[\\\]^_`{|}~])/g, "$1");
}
var Q$ = null;
var Y$ = -1;
var T9 = -1;
function K0($, Q) {
  if (Q >= Y$ && Q <= T9 && $ === Q$) return T9;
  var Z = $.indexOf(`
`, Q), Y = Z < 0 ? $.length : Z;
  return Q$ = $, Y$ = Q, T9 = Y, Y;
}
function s($, Q) {
  let Z = K0($, Q);
  return Z < $.length ? Z + 1 : Z;
}
function H9($, Q, Z) {
  for (; Q < Z; ) {
    let Y = $.charCodeAt(Q);
    if (Y !== N && Y !== b) break;
    Q++;
  }
  return Q;
}
function Q4($, Q) {
  let Z = s($, Q);
  for (; Z < $.length; ) {
    let Y = K0($, Z);
    if (a0($, Z, Y)) return Z;
    Z = s($, Z);
  }
  return $.length;
}
function h5($, Q, Z, Y) {
  let G = 0;
  for (; Q + G < Z && $.charCodeAt(Q + G) === Y; ) G++;
  return G;
}
var V0 = 0;
var $0 = 0;
function P0($, Q, Z) {
  for (V0 = 0, $0 = 0; Q + $0 < Z; ) {
    let Y = $.charCodeAt(Q + $0);
    if (Y === b) V0 += 4 - V0 % 4;
    else if (Y === N) V0++;
    else break;
    $0++;
  }
}
function a0($, Q, Z) {
  return H9($, Q, Z) >= Z;
}
function q6($, Q, Z, Y) {
  let G = K0($, Q);
  if (P0($, Q, G), V0 > 3) return null;
  let V = Q + $0;
  if ($.charCodeAt(V) !== G5) return null;
  let K = h5($, V, G, 35);
  if (K < 1 || K > 6 || (V += K, V < G && $.charCodeAt(V) !== N && $.charCodeAt(V) !== b)) return null;
  V = H9($, V, G);
  for (var X = G; X > V && $.charCodeAt(X - 1) === N; ) X--;
  for (var J = X; X > V && $.charCodeAt(X - 1) === G5; ) X--;
  if (X < J) if (X === V || $.charCodeAt(X - 1) === N) for (; X > V && $.charCodeAt(X - 1) === N; ) X--;
  else X = J;
  let F = $.slice(V, X), U = Q9(F, false, Z, Y), O = ((Y == null ? void 0 : Y.slugify) || f0)(F);
  return { node: { type: _.heading, level: K, children: U, id: O }, end: s($, G) };
}
function W6($, Q, Z) {
  var Y = $.charCodeAt(Q);
  if (Y !== V5 && Y !== d) return false;
  for (var G = Q; G < Z && $.charCodeAt(G) === Y; ) G++;
  for (; G < Z && ($.charCodeAt(G) === N || $.charCodeAt(G) === b); ) G++;
  return G >= Z;
}
function C5($, Q) {
  let Z = K0($, Q);
  if (P0($, Q, Z), V0 > 3) return null;
  let Y = Q + $0, G = $.charCodeAt(Y);
  if (G !== d && G !== A0 && G !== q0) return null;
  let V = 0;
  for (; Y < Z; ) {
    let K = $.charCodeAt(Y);
    if (K === G) V++;
    else if (K !== N && K !== b) return null;
    Y++;
  }
  return V < 3 ? null : { node: { type: _.breakThematic }, end: s($, Z) };
}
var Y4 = 0;
var G4 = 0;
var V4 = "";
function z6($, Q, Z) {
  let Y = K0($, Q);
  if (P0($, Q, Y), V0 > 3) return null;
  let G = V0, V = $0, K = Q + $0, X = $.charCodeAt(K);
  if (X !== e && X !== N0) return null;
  let J = h5($, K, Y, X);
  if (J < 3) return null;
  K += J;
  let F = H9($, K, Y), U = Y;
  if (X === e) {
    for (let m = F; m < Y; m++) if ($.charCodeAt(m) === e) return null;
  }
  for (; U > F && ($.charCodeAt(U - 1) === N || $.charCodeAt(U - 1) === b); ) U--;
  let W = $.slice(F, U), O = "", z = "", B2 = W.indexOf(" ");
  B2 === -1 ? O = W : (O = W.slice(0, B2), z = W.slice(B2 + 1).trim()), O = Z9(O);
  var A = void 0;
  if (z) {
    e$.lastIndex = 0;
    for (var q; (q = e$.exec(z)) !== null; ) A || (A = {}), A[q[1]] = q[2] !== void 0 ? q[2] : q[3];
  }
  let H = s($, Y), j2 = $.length, M = $.length;
  var y;
  X === Y4 && J === G4 ? y = V4 : (y = String.fromCharCode(X).repeat(J), Y4 = X, G4 = J, V4 = y);
  for (var v = H; v < $.length; ) {
    var D = $.indexOf(y, v);
    if (D === -1) break;
    for (var w = D, L = 0; w > 0 && L < 4 && $.charCodeAt(w - 1) === N; ) w--, L++;
    if (L <= 3 && (w === 0 || $.charCodeAt(w - 1) === f)) {
      for (var R = D + J; R < $.length && $.charCodeAt(R) === X; ) R++;
      var I = K0($, R);
      if (a0($, R, I)) {
        j2 = w, M = s($, I);
        break;
      }
    }
    v = D + 1;
  }
  var x;
  if (G === 0) x = j2 > H && $.charCodeAt(j2 - 1) === f ? $.slice(H, j2 - 1) : $.slice(H, j2);
  else {
    x = "";
    for (var k = H; k < j2; ) {
      var u = K0($, k);
      P0($, k, u);
      var E = Math.min($0, G);
      x += $.slice(k + E, u) + `
`, k = s($, u);
    }
    x.length > 0 && x.charCodeAt(x.length - 1) === f && (x = x.slice(0, -1));
  }
  return { node: { type: _.codeBlock, lang: O || void 0, text: x, infoString: z || void 0, attrs: A }, end: M };
}
function K4($, Q, Z) {
  for (var Y = 0, G = Q; G < Z && Y < 4; ) $.charCodeAt(G) === b ? Y += 4 - Y % 4 : Y++, G++;
  if (Y < 4) return "";
  for (var V = "", K = 4; K < Y; K++) V += " ";
  return V + $.slice(G, Z);
}
function O6($, Q) {
  let Z = K0($, Q);
  if (P0($, Q, Z), V0 < 4) return null;
  let Y = "", G = Q;
  for (; G < $.length; ) {
    let O = K0($, G);
    if (P0($, G, O), a0($, G, O)) {
      for (var V = K4($, G, O) + `
`, K = s($, O); K < $.length; ) {
        var X = K0($, K);
        if (a0($, K, X)) {
          V += K4($, K, X) + `
`, K = s($, X);
          continue;
        }
        P0($, K, X), V0 >= 4 && (Y += V, G = K);
        break;
      }
      if (G !== K) break;
      continue;
    }
    if (V0 < 4) break;
    let z = 0, B2 = 0;
    var J = 0;
    for (let A = G; A < O && B2 < 4; A++) {
      if ($.charCodeAt(A) === b) {
        var F = 4 - B2 % 4;
        B2 + F > 4 && (J = B2 + F - 4), B2 += F;
      } else B2++;
      z++;
    }
    var U = "";
    if (J > 0) for (var W = 0; W < J; W++) U += " ";
    U += $.slice(G + z, O), Y += U + `
`, G = s($, O);
  }
  for (; Y.length > 0 && Y.charCodeAt(Y.length - 1) === f; ) Y = Y.slice(0, -1);
  return Y ? { node: { type: _.codeBlock, lang: void 0, text: Y, infoString: void 0, attrs: void 0 }, end: G } : null;
}
function H6($, Q, Z, Y) {
  let G = K0($, Q);
  if (P0($, Q, G), V0 > 3) return null;
  let V = Q + $0;
  if ($.charCodeAt(V) !== o) return null;
  let K = "", X = Q, J, F = false, U = false, W = false;
  for (; X < $.length; ) {
    let S = K0($, X);
    P0($, X, S);
    let a = X + $0;
    if ($.charCodeAt(a) === o) {
      let g = a + 1;
      var O = V0 + 1, z = false;
      if (g < S) {
        var B2 = $.charCodeAt(g);
        B2 === N ? (g++, O++, z = true) : B2 === b && (z = true);
      }
      for (var A = "", q = false, H = g; H < S; H++) if ($.charCodeAt(H) === b) {
        q = true;
        break;
      }
      if (q) {
        var j2 = O;
        if (z && g < S && $.charCodeAt(g) === b) {
          for (var M = 4 - j2 % 4, y = 0; y < M - 1; y++) A += " ";
          j2 += M, g++;
        }
        for (var v = g; v < S; v++) if ($.charCodeAt(v) === b) {
          for (var D = 4 - j2 % 4, w = 0; w < D; w++) A += " ";
          j2 += D;
        } else A += $[v], j2++;
      } else A = $.slice(g, S);
      if (!K && !J) {
        let X0 = A.match(/^\[!([A-Za-z]+)\]\s*$/);
        if (X0) {
          J = X0[1].toUpperCase(), X = s($, S);
          continue;
        }
      }
      K += A + `
`;
      var L = A.trimStart();
      L.startsWith("```") || L.startsWith("~~~") ? W = !W : (A.startsWith("    ") || A.startsWith("	")) && (W = true), U = L.length > 0, X = s($, S);
    } else if (K && !a0($, X, S) && U) {
      if (V0 < 4) {
        var R = X + $0, I = R < S ? $.charCodeAt(R) : 0;
        if (I === G5 || I === o || I === e || I === N0 || I === Q0 || (I === d || I === A0 || I === q0) && C5($, X) || (I === d || I === A0 || I === b5) && R + 1 < S && ($.charCodeAt(R + 1) === N || $.charCodeAt(R + 1) === b)) break;
        if (I >= r && I <= i) {
          for (var x = R; x < S && $.charCodeAt(x) >= r && $.charCodeAt(x) <= i; ) x++;
          if (x < S && ($.charCodeAt(x) === s0 || $.charCodeAt(x) === q5)) break;
        }
      }
      if (W) break;
      K += $.slice(X, S) + `
`, F = true, X = s($, S);
    } else break;
  }
  if (!K && !J) return null;
  var { inBlockQuote: k, g: u } = Z;
  Z.inBlockQuote = true, F && (Z.g = true);
  var E = Z.e;
  Z.e = E || !a0($, X, $.length);
  let m = m5(K || "", Z, Y);
  return Z.e = E, Z.inBlockQuote = k, Z.g = u, { node: { type: _.blockQuote, children: m, alert: J || void 0 }, end: X };
}
function _4($, Q, Z) {
  for (var Y = 0, G = Q; G < Z; G++) $.charCodeAt(G) === b ? Y += 4 - Y % 4 : Y++;
  return Y;
}
function x5($, Q, Z) {
  if (P0($, Q, Z), V0 > 3) return null;
  var Y = Q + $0;
  if (Y >= Z) return null;
  var G = $.charCodeAt(Y), V = V0, K = Y, X = Y;
  if (G === d || G === A0 || G === b5) {
    if (K = Y + 1, K < Z && $.charCodeAt(K) !== N && $.charCodeAt(K) !== b && $.charCodeAt(K) !== f) return null;
  } else if (G >= r && G <= i) {
    for (; X < Z && X - Y < 9; ) {
      var J = $.charCodeAt(X);
      if (J < r || J > i) break;
      X++;
    }
    if (X > Y && X < Z) {
      var F = $.charCodeAt(X);
      if (F === s0 || F === q5) {
        if (K = X + 1, K < Z && $.charCodeAt(K) !== N && $.charCodeAt(K) !== b && $.charCodeAt(K) !== f) return null;
      } else return null;
    } else return null;
  } else return null;
  var U = K, W = _4($, Q, K), O = 0, z = U, B2 = W;
  if (U >= Z) return { ordered: G >= r && G <= i, marker: G >= r && G <= i ? $[X] : $[Y], start: G >= r && G <= i ? parseInt($.slice(Y, X), 10) : void 0, contentStart: U, contentCol: W + 1, markerCol: V, isEmpty: true };
  for (; z < Z && ($.charCodeAt(z) === N || $.charCodeAt(z) === b); ) {
    if ($.charCodeAt(z) === b) {
      var A = 4 - B2 % 4;
      B2 += A;
    } else B2++;
    z++, O++;
  }
  var q = z >= Z, H = B2 - W;
  return q || H > 4 ? (B2 = W + 1, z = U + 1, O = 1) : O === 0 && (B2 = W + 1, z = U, O = 1), { ordered: G >= r && G <= i, marker: G >= r && G <= i ? $[X] : $[Y], start: G >= r && G <= i ? parseInt($.slice(Y, X), 10) : void 0, contentStart: z, contentCol: B2, markerCol: V, isEmpty: q };
}
var $9 = 0;
function X4($, Q, Z, Y) {
  var G = 0, V = Q;
  for ($9 = 0; V < Z && G < Y; ) {
    var K = $.charCodeAt(V);
    if (K === b) {
      var X = 4 - G % 4;
      if (G + X > Y) {
        $9 = G + X - Y, V++, G = Y;
        break;
      }
      G += X;
    } else if (K === N) G++;
    else break;
    V++;
  }
  return V;
}
function J4($, Q, Z, Y) {
  var G = K0($, Q), V = x5($, Q, G);
  if (!V) return null;
  var K = [], X = Q, J = V.contentCol, F = "", U = V.isEmpty, W = false, O = false;
  if (!V.isEmpty) {
    for (var z = false, B2 = V.contentStart; B2 < G; B2++) if ($.charCodeAt(B2) === b) {
      z = true;
      break;
    }
    if (z) {
      var A = "", q = _4($, Q, V.contentStart), H = q - V.contentCol;
      if (H > 0) for (var j2 = 0; j2 < H; j2++) A += " ";
      for (var M = V.contentStart; M < G; M++) if ($.charCodeAt(M) === b) {
        for (var y = 4 - q % 4, v = 0; v < y; v++) A += " ";
        q += y;
      } else A += $[M], q++;
      F = A + `
`;
    } else F = $.slice(V.contentStart, G) + `
`;
  }
  for (X = s($, G); X < $.length; ) {
    var D = K0($, X);
    P0($, X, D);
    var w = $.charCodeAt(X + $0);
    if (V0 < J && (w === d || w === A0 || w === q0) && V0 <= 3 && C5($, X)) break;
    var L = x5($, X, D);
    if (L && L.ordered === V.ordered && L.marker === V.marker && L.markerCol < J) {
      K.push({ contentCol: J, raw: F, hasBlankAfter: W, isEmpty: U }), W && (O = true), J = L.contentCol, U = L.isEmpty, W = false, F = L.isEmpty ? "" : $.slice(L.contentStart, D) + `
`, X = s($, D);
      continue;
    }
    if (a0($, X, D)) {
      var R = X4($, X, D, J);
      if ($9 > 0 || R < D) {
        for (var I = "", x = 0; x < $9; x++) I += " ";
        F += I + $.slice(R, D) + `
`;
      } else F += `
`;
      X = s($, D);
      for (var k = false, u = 0; u < F.length; u++) {
        var E = F.charCodeAt(u);
        if (E !== f && E !== N5 && E !== N && E !== b) {
          k = true;
          break;
        }
      }
      if (U && !k) if (X < $.length) {
        var m = K0($, X), T = x5($, X, m);
        if (!T || T.ordered !== V.ordered || T.marker !== V.marker) break;
        W = true;
      } else break;
      if (X < $.length) {
        var S = K0($, X);
        P0($, X, S);
        var a = $.charCodeAt(X + $0);
        if ((a === d || a === A0 || a === q0) && V0 <= 3 && C5($, X)) break;
        var g = x5($, X, S);
        if (g && g.ordered === V.ordered && g.marker === V.marker && g.markerCol < J) {
          W = true;
          continue;
        }
        if (!a0($, X, S) && V0 < J) break;
      }
      continue;
    }
    if (V0 >= J) {
      var X0 = X4($, X, D, J);
      if ($9 > 0) {
        for (var n = "", y0 = J, C0 = 0; C0 < $9; C0++) n += " ", y0++;
        for (var m0 = X0; m0 < D; m0++) if ($.charCodeAt(m0) === b) {
          for (var t = 4 - y0 % 4, l = 0; l < t; l++) n += " ";
          y0 += t;
        } else n += $[m0], y0++;
        F += n + `
`;
      } else F += $.slice(X0, D) + `
`;
      X = s($, D);
      continue;
    }
    for (var j0 = false, Z0 = 0; Z0 < F.length; Z0++) {
      var Y0 = F.charCodeAt(Z0);
      if (Y0 !== f && Y0 !== N5 && Y0 !== N && Y0 !== b) {
        j0 = true;
        break;
      }
    }
    if (!W && j0 && !U) {
      var w0 = X + $0, O0 = $.charCodeAt(w0), M0 = O0 === G5 || O0 === o || O0 === Q0 || O0 === e || O0 === N0 || (O0 === d || O0 === A0 || O0 === q0 || O0 === b5) && (C5($, X) !== null || x5($, X, D) !== null) || O0 >= r && O0 <= i && x5($, X, D) !== null;
      if (!M0) {
        F += "" + $.slice(w0, D) + `
`, X = s($, D);
        continue;
      }
    }
    break;
  }
  if (K.push({ contentCol: J, raw: F, hasBlankAfter: W, isEmpty: U }), K.length === 0) return null;
  var _0 = O;
  if (!_0) for (var R0 = 0; R0 < K.length; R0++) {
    if (K[R0].hasBlankAfter && R0 < K.length - 1) {
      _0 = true;
      break;
    }
    if (!K[R0].isEmpty) {
      for (var I0 = K[R0].raw, J0 = I0.length, h = 0, B0 = false, L0 = false, v0 = false, e0 = false, O5 = 0, H5 = 0, l0 = -1; h < J0; ) {
        var C = I0.indexOf(`
`, h);
        if (C < 0 && (C = J0), e0) {
          P0(I0, h, C);
          for (var $5 = I0.slice(h + $0, C), P = 0; P < $5.length && $5.charCodeAt(P) === O5; ) P++;
          P >= H5 && $5.slice(P).trim() === "" && (e0 = false), h = C < J0 ? C + 1 : J0;
          continue;
        }
        if (a0(I0, h, C)) {
          l0 >= 0 ? v0 = true : B0 && (L0 = true), h = C < J0 ? C + 1 : J0;
          continue;
        }
        if (P0(I0, h, C), l0 >= 0) {
          if (V0 >= l0) {
            h = C < J0 ? C + 1 : J0;
            continue;
          }
          var F0 = x5(I0, h, C);
          if (F0 && F0.markerCol < l0 && F0.contentCol <= l0) {
            h = C < J0 ? C + 1 : J0;
            continue;
          }
          if (F0) {
            h = C < J0 ? C + 1 : J0;
            continue;
          }
          l0 = -1, v0 && (L0 = true, v0 = false);
        }
        var h0 = I0.slice(h + $0, C), Z5 = h0.charCodeAt(0);
        if ((Z5 === e || Z5 === N0) && V0 <= 3) {
          for (var B5 = 0; B5 < h0.length && h0.charCodeAt(B5) === Z5; ) B5++;
          if (B5 >= 3) {
            if (L0 && B0) {
              _0 = true;
              break;
            }
            e0 = true, O5 = Z5, H5 = B5, B0 = true, h = C < J0 ? C + 1 : J0;
            continue;
          }
        }
        var o0 = V0 <= 3 ? x5(I0, h, C) : null;
        if (o0 && B0) {
          if (L0) {
            _0 = true;
            break;
          }
          l0 = o0.contentCol, v0 = false, h = C < J0 ? C + 1 : J0, B0 = true;
          continue;
        }
        if (L0) {
          _0 = true;
          break;
        }
        B0 = true, h = C < J0 ? C + 1 : J0;
      }
      if (_0) break;
    }
  }
  for (var D0 = [], k0 = 0; k0 < K.length; k0++) {
    for (var Q5 = K[k0], x0 = Q5.raw, Y5 = x0.length; Y5 > 0 && x0.charCodeAt(Y5 - 1) === f; ) Y5--;
    var r0 = Y5 < x0.length ? x0.slice(0, Y5) : x0, D5 = null;
    if (r0.length >= 3 && r0.charCodeAt(0) === z0) {
      var n0 = r0[1];
      (n0 === " " || n0 === "x" || n0 === "X") && r0.charCodeAt(2) === K5 && (D5 = { type: _.gfmTask, completed: n0 === "x" || n0 === "X" }, r0 = r0.slice(3));
    }
    var W0;
    if (Q5.isEmpty && r0.trim() === "") W0 = [];
    else if (_0) {
      var j9 = Z.inList;
      Z.inList = true;
      var n5 = Z.e;
      Z.e = n5 || k0 !== K.length - 1, W0 = m5(r0, Z, Y), Z.e = n5, Z.inList = j9;
    } else {
      var I5 = Z.inList;
      Z.inList = true;
      var M9 = Z.e;
      if (Z.e = M9 || k0 !== K.length - 1, W0 = m5(r0, Z, Y), Z.e = M9, Z.inList = I5, W0.length === 1 && W0[0].type === _.paragraph) W0 = W0[0].children;
      else if (Z.k) {
        var _9 = [];
        Z.k.push({ src: W0, dest: _9, unwrap: true }), W0 = _9;
      } else {
        for (var A5 = [], T0 = 0; T0 < W0.length; T0++) if (W0[T0].type === _.paragraph) for (var v5 = W0[T0].children, Y9 = 0; Y9 < v5.length; Y9++) A5.push(v5[Y9]);
        else A5.push(W0[T0]);
        W0 = A5;
      }
    }
    if (D5) {
      var G9 = [D5, { type: _.text, text: " " }];
      if (Z.k) Z.k.push({ src: W0, dest: G9, unwrap: false });
      else for (var V9 = 0; V9 < W0.length; V9++) G9.push(W0[V9]);
      D0.push(G9);
    } else D0.push(W0);
  }
  return { node: { type: V.ordered ? _.orderedList : _.unorderedList, start: V.ordered ? V.start : void 0, items: D0 }, end: X };
}
var x9 = /* @__PURE__ */ new Set(["address", "article", "aside", "base", "basefont", "blockquote", "body", "caption", "center", "col", "colgroup", "dd", "details", "dialog", "dir", "div", "dl", "dt", "fieldset", "figcaption", "figure", "footer", "form", "frame", "frameset", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hr", "html", "iframe", "legend", "li", "link", "main", "menu", "menuitem", "nav", "noframes", "ol", "optgroup", "option", "p", "param", "search", "section", "summary", "table", "tbody", "td", "tfoot", "th", "thead", "title", "tr", "track", "ul"]);
function L5($, Q, Z) {
  let Y = {};
  for (let [K, X] of Object.entries($)) {
    let J = K.toLowerCase();
    if (J === "style" && typeof X == "string") {
      let F = {}, U = [], W = 0, O = 0;
      for (let B2 = 0; B2 < X.length; B2++) {
        let A = X.charCodeAt(B2);
        A === U5 ? W++ : A === q5 ? W-- : A === W9 && W === 0 && (U.push(X.slice(O, B2)), O = B2 + 1);
      }
      O < X.length && U.push(X.slice(O));
      let z = false;
      U.forEach((B2) => {
        let A = B2.indexOf(":");
        if (A === -1) return;
        let q = B2.slice(0, A).trim(), H = B2.slice(A + 1).trim();
        if (q && H) {
          if (/url\s*\(\s*(javascript|vbscript|data:(?!image\/))/i.test(H)) {
            z = true;
            return;
          }
          let j2 = q.indexOf("-") !== -1 ? q.replace(/-([a-z])/g, (M, y) => y.toUpperCase()) : q;
          F[j2] = H;
        }
      }), !z && Object.keys(F).length > 0 && (Y[K] = F);
    } else if ((J === "href" || J === "src") && (Z == null ? void 0 : Z.sanitizer)) {
      let F = Z.sanitizer(X, Q, J);
      F !== null && (Y[K] = F);
    } else if (X === "") Y[K] = true;
    else if (X.length >= 2 && X.charCodeAt(0) === y5 && X.charCodeAt(X.length - 1) === R9) {
      var G = X.slice(1, -1);
      if (G.length > 0) {
        var V = G.charCodeAt(0);
        if (V === z0 || V === y5) try {
          Y[K] = JSON.parse(G);
          continue;
        } catch {
        }
      }
      if (G === "true") {
        Y[K] = true;
        continue;
      }
      if (G === "false") {
        Y[K] = false;
        continue;
      }
      if (Z == null ? void 0 : Z.evalUnserializableExpressions) try {
        Y[K] = (0, eval)("(" + G + ")");
        continue;
      } catch {
      }
      Y[K] = G;
    } else Y[K] = X;
  }
  return Y;
}
function f5($, Q, Z) {
  let Y = Q.length;
  if (Y === 0) return Z;
  var G = Q.charCodeAt(0), V = G >= p && G <= c || G >= G0 && G <= U0;
  if (!V) {
    for (var K = String.fromCharCode(G), X = $.length - Y, J = Z; J <= X; ) {
      var F = $.indexOf(K, J);
      if (F === -1 || F > X) return -1;
      for (var U = true, W = 1; W < Y; W++) {
        var O = $.charCodeAt(F + W), z = Q.charCodeAt(W);
        if (O >= p && O <= c && (O += X5), z >= p && z <= c && (z += X5), O !== z) {
          U = false;
          break;
        }
      }
      if (U) return F;
      J = F + 1;
    }
    return -1;
  }
  G >= p && G <= c && (G += X5);
  for (let A = Z; A <= $.length - Y; A++) {
    var B2 = $.charCodeAt(A);
    if (B2 >= p && B2 <= c && (B2 += X5), B2 !== G) continue;
    let q = true;
    for (let H = 1; H < Y; H++) {
      let j2 = $.charCodeAt(A + H), M = Q.charCodeAt(H);
      if (j2 >= p && j2 <= c && (j2 += X5), M >= p && M <= c && (M += X5), j2 !== M) {
        q = false;
        break;
      }
    }
    if (q) return A;
  }
  return -1;
}
function B6($, Q, Z) {
  let Y = Q.length;
  var G = Q.charCodeAt(0);
  G >= p && G <= c && (G += X5);
  for (let K = Math.min(Z, $.length - Y); K >= 0; K--) {
    var V = $.charCodeAt(K);
    if (V >= p && V <= c && (V += X5), V !== G) continue;
    let X = true;
    for (let J = 1; J < Y; J++) {
      let F = $.charCodeAt(K + J), U = Q.charCodeAt(J);
      if (F >= p && F <= c && (F += X5), U >= p && U <= c && (U += X5), F !== U) {
        X = false;
        break;
      }
    }
    if (X) return K;
  }
  return -1;
}
var g9 = -1;
function G$($, Q, Z) {
  let Y = Z.toLowerCase(), G = "<" + Y, V = "</" + Y, K = 1, X = Q, J = $.length;
  for (g9 = -1; X < J && K > 0; ) {
    let W = f5($, G, X), O = f5($, V, X);
    if (O === -1) return -1;
    if (W !== -1 && W < O) {
      let z = M5($, W);
      z ? (z.tag.toLowerCase() === Y && !z.isClosing && !z.selfClosing && !r5(z.tag) && K++, X = z.end) : X = W + 1;
    } else {
      var F = O + V.length, U = F < J ? $.charCodeAt(F) : 62;
      if ((U === o || U === N || U === b || U === f) && (K--, K === 0)) {
        g9 = O;
        let z = O + V.length;
        for (; z < J && $.charCodeAt(z) !== o; ) z++;
        return z + 1;
      }
      X = O + 1;
    }
  }
  return -1;
}
function I4($, Q, Z, Y) {
  if (Y.ignoreHTMLBlocks || Y.disableParsingRawHTML) return null;
  var G = K0($, Q);
  if (P0($, Q, G), V0 > 3 && !Z.inHTML) return null;
  var V = Q + $0;
  if ($.charCodeAt(V) !== Q0) return null;
  var K = $.indexOf(">", V + 1);
  if (K !== -1 && K < G) {
    var X = $.slice(V + 1, K);
    if (/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(X) || /^[^\s@]+@[^\s@]+$/.test(X)) return null;
  }
  var J = D6($, V);
  if (J >= 1 && J <= 5) {
    var F = $.length;
    if (J === 1) {
      for (var U = $.length, W = 0; W < $$.length; W++) {
        var O = f5($, "</" + $$[W] + ">", V);
        O >= 0 && O < U && (U = O);
      }
      if (U < $.length) {
        var z = $.indexOf(">", U);
        F = z >= 0 ? s($, z + 1) : $.length;
      }
    } else {
      var B2 = J === 2 ? "-->" : J === 3 ? "?>" : J === 4 ? ">" : "]]>", A = $.indexOf(B2, V);
      A >= 0 && (F = s($, A + B2.length));
    }
    var q = $.slice(V, F);
    if (J >= 2) return { node: { type: _.htmlComment, text: q, l: false, raw: true }, end: F };
    var H = "div", j2 = q.match(/^<\/?([a-zA-Z][a-zA-Z0-9-]*)/);
    j2 && (H = j2[1]);
    var M = M5($, V), y = {}, v;
    M && !M.isClosing && (y = L5(M.attrs, H, Y), v = M.whitespaceBeforeAttrs + M.rawAttrs);
    var D = [], w = H.toLowerCase(), L = "</" + w, R = f5(q, L, 0), I = q, x = "";
    if (M && M.isClosing) for (I = q.slice(M.end - V); I.length > 0 && I.charCodeAt(I.length - 1) === f; ) I = I.slice(0, -1);
    else if (M && !M.isClosing) {
      var k = M.end - V;
      if (R !== -1) for (I = q.slice(k), I.charCodeAt(0) === f && (I = I.slice(1)); I.length > 0 && I.charCodeAt(I.length - 1) === f; ) I = I.slice(0, -1);
      else for (I = q; I.length > 0 && I.charCodeAt(I.length - 1) === f; ) I = I.slice(0, -1);
      if (R !== -1) {
        var u = q.slice(k, R);
        x = u.trim();
      }
    }
    var E = M ? M.isClosing : false;
    return { node: { type: _.htmlBlock, tag: H, attrs: y, i: v, children: D, a: g5(I), text: x, d: true, b: E }, end: F };
  }
  if (J === 6 || J === 7) {
    var m = Q4($, Q), T = m < $.length ? m : $.length, S = $.slice(V, T), a = m < $.length ? s($, m) : $.length, g = M5($, V);
    if (g) {
      var X0 = g.tag, n = X0.toLowerCase(), y0 = g.isClosing;
      if (y0) {
        var C0 = $.slice(g.end, T);
        return { node: { type: _.htmlBlock, tag: X0, attrs: {}, children: [], a: g5(C0), text: C0, d: true, b: true }, end: a };
      }
      if (g.selfClosing || r5(X0)) return { node: { type: _.htmlBlock, tag: X0, attrs: L5(g.attrs, X0, Y), i: g.whitespaceBeforeAttrs + g.rawAttrs, children: [], a: "", text: "", d: false, b: false }, end: g.end < $.length && $.charCodeAt(g.end) === f ? g.end + 1 : g.end };
      var m0 = Z.c || 0, t = $.slice(V, T), l = -1, j0 = -1, Z0 = false;
      if (m0 < 10) {
        for (var Y0 = "</" + n, w0 = g.end - V, O0 = 1, M0 = w0; M0 < t.length && O0 > 0; ) {
          var _0 = f5(t, "<" + n, M0), R0 = f5(t, Y0, M0);
          if (R0 === -1) break;
          if (_0 !== -1 && _0 < R0) {
            var I0 = _0 + n.length + 1;
            if (I0 < t.length) {
              var J0 = t.charCodeAt(I0);
              (J0 === N || J0 === b || J0 === f || J0 === o || J0 === H0) && O0++;
            }
            M0 = _0 + 1;
          } else {
            var h = R0 + Y0.length;
            if (h < t.length) {
              var B0 = t.charCodeAt(h);
              if ((B0 === o || B0 === N || B0 === b || B0 === f) && (O0--, O0 === 0)) {
                l = R0;
                for (var L0 = h; L0 < t.length && t.charCodeAt(L0) !== o; ) L0++;
                j0 = L0 + 1;
                break;
              }
            } else if (O0--, O0 === 0) {
              l = R0, j0 = t.length;
              break;
            }
            M0 = R0 + 1;
          }
        }
        if (l === -1 && J === 6 && !g.isClosing) {
          var v0 = G$($, g.end, n);
          if (v0 !== -1) {
            for (var e0 = g9, O5 = g.end, H5 = false, l0 = false, C = false, $5 = 0, P = false, F0 = false, h0 = O5; h0 < e0; ) {
              var Z5 = $.charCodeAt(h0);
              if (Z5 === f) F0 && (H5 = true), P || $5++, F0 = true, h0++;
              else if (Z5 === N || Z5 === b) h0++;
              else {
                if (!P && (P = true, $5 >= 2 && Z5 === Q0)) {
                  C = true;
                  break;
                }
                if (F0 = false, Z5 === Q0) {
                  var B5 = $.charCodeAt(h0 + 1) | 32;
                  if ((B5 === y9 || B5 === c9 || B5 === p5) && (s$.lastIndex = h0, s$.test($))) {
                    l0 = true;
                    break;
                  }
                }
                h0++;
              }
            }
            if (H5 && !C && !l0) {
              var o0 = K0($, v0);
              T = o0, a = s($, o0), t = $.slice(V, T), S = $.slice(V, T), l = e0 - V, j0 = v0 - V, Z0 = true;
            }
          }
        }
      }
      var D0 = g.rawAttrs.indexOf(`
`) !== -1 || g.whitespaceBeforeAttrs.indexOf(`
`) !== -1, k0 = false;
      if (l !== -1) {
        var Q5 = t.slice(j0).trim();
        k0 = Q5.length === 0;
      }
      var x0 = false, Y5 = T, r0 = a;
      if (l !== -1) {
        var D5 = V + j0, n0 = K0($, D5 - 1);
        if (D5 < n0) {
          for (var W0 = D5; W0 < n0 && ($.charCodeAt(W0) === N || $.charCodeAt(W0) === b); ) W0++;
          if (W0 < n0 && $.charCodeAt(W0) === Q0) {
            var j9 = M5($, W0);
            j9 && !j9.isClosing && (x0 = true, Y5 = D5, r0 = D5, k0 = true);
          }
        }
        if (!x0) {
          var n5 = s($, n0);
          if (n5 < T) {
            for (var I5 = n5; I5 < T && ($.charCodeAt(I5) === N || $.charCodeAt(I5) === b); ) I5++;
            if (I5 < T && $.charCodeAt(I5) === Q0) {
              var M9 = M5($, I5);
              M9 && (x0 = true, Y5 = n0, r0 = n5, k0 = true);
            }
          }
        }
        if (!x0 && Z.inHTML) {
          x0 = true, Y5 = n0, r0 = s($, n0);
          var _9 = $.slice(D5, n0).trim();
          k0 = _9.length === 0;
        }
      }
      var A5 = [], T0 = "";
      if (l !== -1) {
        T0 = t.slice(g.end - V, l);
        var v5 = T0.trim();
        if (v5) {
          var { inline: Y9, inHTML: G9, c: V9 } = Z;
          Z.inHTML = true, Z.c = m0 + 1;
          var y4 = n === "p";
          if (y4) Z.inline = true, A5 = Q9(v5, false, Z, Y);
          else {
            var W$ = T0.indexOf(`

`) !== -1, z$ = e9.test(v5), C9 = t$.test(v5), R4 = W$ || z$ || Z.inHTML && C9, L4 = T0.length >= 2 && T0.charCodeAt(0) === f && T0.charCodeAt(T0.length - 1) === f && !W$;
            L4 && !z$ && !C9 ? A5 = [{ type: _.text, text: v5 }] : R4 || C9 ? (Z.inline = false, A5 = m5(T0, Z, Y)) : (Z.inline = true, A5 = Q9(v5, false, Z, Y));
          }
          Z.inline = Y9, Z.inHTML = G9, Z.c = V9;
        }
      }
      var O$ = false;
      if (J === 6 && l !== -1 && !Z.inHTML && !D0) {
        var k4 = /<[a-zA-Z][^>]*>/.test(T0), S4 = T0.indexOf(`

`) !== -1 || e9.test(T0);
        k4 && !S4 && (O$ = true);
      }
      var E4 = !Z0 && (Z.inHTML || J === 7 || D0 || !k0 || O$);
      if (E4) {
        var J5, H$ = false;
        if (l !== -1 && x0) {
          var B$ = false;
          if (Z.inHTML && j0 < t.length) {
            for (var I9 = j0; I9 < t.length && t.charCodeAt(I9) !== f; ) I9++;
            var m9 = t.slice(j0, I9).trim();
            B$ = m9.length > 1 && m9.charCodeAt(0) === Q0 && m9.charCodeAt(1) !== H0;
          }
          J5 = B$ ? t.slice(g.end - V) : T0, H$ = true;
        } else (J === 7 || Z.inHTML) && l !== -1 ? (J5 = t.slice(g.end - V), J5.charCodeAt(0) === f && (J5 = J5.slice(1))) : x0 ? J5 = $.slice(V, Y5) : D0 ? J5 = S : (J5 = t.slice(g.end - V), J5.charCodeAt(0) === f && (J5 = J5.slice(1)));
        var D$ = g5(J5), A$ = { type: _.htmlBlock, tag: X0, attrs: L5(g.attrs, X0, Y), i: g.whitespaceBeforeAttrs + g.rawAttrs, children: A5, a: D$, text: D$, d: true, b: false };
        return H$ && (A$.o = true), { node: A$, end: r0 };
      }
      var j$ = g5(T0);
      return { node: { type: _.htmlBlock, tag: X0, attrs: L5(g.attrs, X0, Y), i: g.whitespaceBeforeAttrs + g.rawAttrs, children: A5, a: Z0 ? "" : j$, text: j$, d: false, b: false }, end: r0 };
    }
    var v9 = S.match(/^<(\/?)([a-zA-Z][a-zA-Z0-9-]*)/), T4 = v9 ? v9[2] : "div", M$ = v9 ? v9[1] === "/" : false, h9 = S;
    if (M$) {
      var _$ = S.indexOf(">");
      _$ !== -1 && (h9 = S.slice(_$ + 1));
    }
    return { node: { type: _.htmlBlock, tag: T4, attrs: {}, children: [], a: g5(h9), text: h9, d: true, b: M$ }, end: a };
  }
  var u0 = M5($, V);
  if (!u0) return null;
  var k5 = u0.tag, u9 = k5.toLowerCase(), I$ = k5.charCodeAt(0), N9 = I$ >= p && I$ <= c;
  if (!N9 && !x9.has(u9) && !B9.has(u9) && !u9.includes("-")) return null;
  if (u0.isClosing) return { node: { type: _.htmlSelfClosing, tag: k5, attrs: {}, a: $.slice(V, u0.end), b: true }, end: u0.end };
  var u5 = G$($, u0.end, k5), K9 = [];
  if (u5 !== -1) {
    var x4 = g9, P9 = $.slice(u0.end, x4), w9 = P9.trim();
    if (w9) {
      var g4 = P9.indexOf(`

`) !== -1, f4 = e9.test(w9), C4 = t$.test(w9), m4 = Z.inline, h4 = Z.inHTML, u4 = Z.c;
      Z.inHTML = true, Z.c = (Z.c || 0) + 1, g4 || f4 || C4 ? (Z.inline = false, K9 = m5(P9, Z, Y)) : (Z.inline = true, K9 = Q9(w9, false, Z, Y)), Z.inline = m4, Z.inHTML = h4, Z.c = u4;
    }
    var p4 = K0($, u5), d4 = $.slice(u5, p4).trim(), p9 = d4 ? u5 : s($, u5), v$ = N9 ? $.slice(V, u5) : $.slice(V, p9), m6 = N9 ? u5 : p9;
    return { node: { type: _.htmlBlock, tag: k5, attrs: L5(u0.attrs, k5, Y), i: u0.whitespaceBeforeAttrs + u0.rawAttrs, children: K9, a: g5(v$), text: N9 ? P9 : v$, d: true, b: false }, end: p9 };
  }
  var X9 = Q4($, u0.end), c4 = X9 < $.length ? s($, X9) : X9, d9 = $.slice(u0.end, X9);
  if (d9.trim()) {
    var { inline: l4, inHTML: o4, c: r4 } = Z;
    Z.inline = false, Z.inHTML = true, Z.c = (Z.c || 0) + 1, K9 = m5(d9, Z, Y), Z.inline = l4, Z.inHTML = o4, Z.c = r4;
  }
  var i4 = $.slice(u0.end, X9);
  return { node: { type: _.htmlBlock, tag: k5, attrs: L5(u0.attrs, k5, Y), i: u0.whitespaceBeforeAttrs + u0.rawAttrs, children: K9, a: g5(i4), text: d9, d: true, b: false }, end: c4 };
}
function D6($, Q) {
  if ($.charCodeAt(Q) !== Q0) return 0;
  var Z = Q + 1, Y = $.length;
  if ($.charCodeAt(Z) === p0 && $.charCodeAt(Z + 1) === d && $.charCodeAt(Z + 2) === d) return 2;
  if ($.charCodeAt(Z) === s5) return 3;
  if ($.charCodeAt(Z) === p0) {
    var G = $.charCodeAt(Z + 1);
    if (G >= p && G <= c) return 4;
    if ($.slice(Z + 1, Z + 8) === "[CDATA[") return 5;
  }
  for (var V = $.charCodeAt(Z) === H0, K = V ? Z + 1 : Z, X = K; X < Y; ) {
    var J = $.charCodeAt(X);
    if (J >= p && J <= c || J >= G0 && J <= U0 || J >= r && J <= i || J === d) X++;
    else break;
  }
  if (X === K) return 0;
  var F = $.slice(K, X);
  if (B9.has(F.toLowerCase())) {
    if (V) return 0;
    var U = $.charCodeAt(X);
    return U === N || U === b || U === o || U === f || X >= Y ? 1 : 0;
  }
  if (x9.has(F.toLowerCase())) {
    if (V) {
      for (var W = X; W < Y && ($.charCodeAt(W) === N || $.charCodeAt(W) === b); ) W++;
      return W < Y && $.charCodeAt(W) === o ? 6 : 0;
    }
    var O = X < Y ? $.charCodeAt(X) : -1;
    return O === N || O === b || O === o || O === f || O === H0 || O === -1 ? 6 : 0;
  }
  if (V) {
    for (var q = X; q < Y && ($.charCodeAt(q) === N || $.charCodeAt(q) === b); ) q++;
    if (q < Y && $.charCodeAt(q) === o) {
      var H = K0($, Q), j2 = $.slice(q + 1, H).trim();
      if (j2 === "") return 7;
    }
  } else {
    var z = K0($, Q), B2 = M5($, Q);
    if (B2 && B2.end <= z) {
      var A = $.slice(B2.end, z).trim();
      if (A === "") return 7;
    }
  }
  return 0;
}
function F4($, Q, Z) {
  for (var Y = 0, G = $.length; Y < G && ($.charCodeAt(Y) === N || $.charCodeAt(Y) === b); ) Y++;
  for (; G > Y && ($.charCodeAt(G - 1) === N || $.charCodeAt(G - 1) === b); ) G--;
  Y < G && $.charCodeAt(Y) === S0 && Y++, G > Y && $.charCodeAt(G - 1) === S0 && (G - 2 < Y || $.charCodeAt(G - 2) !== b0) && G--;
  for (var V = [], K = Y, X = false, J = [], F = Y; F < G; ) {
    var U = $.charCodeAt(F);
    if (U === b0 && F + 1 < G) {
      $.charCodeAt(F + 1) === S0 ? (X || (X = true, J = []), J.push($.slice(K, F)), J.push("|"), F += 2, K = F) : F += 2;
      continue;
    }
    if (U === e) {
      for (var W = 0; F < G && $.charCodeAt(F) === e; ) W++, F++;
      for (var O = false; F < G && !O; ) {
        for (var z = 0; F < G && $.charCodeAt(F) === e; ) z++, F++;
        z === W ? O = true : z === 0 && F++;
      }
      continue;
    }
    if (U === S0) {
      var B2 = X ? (J.push($.slice(K, F)), J.join("")) : $.slice(K, F);
      V.push(B2.trim()), F++, K = F, X = false, J = [];
      continue;
    }
    F++;
  }
  var A = X ? (J.push($.slice(K, G)), J.join("")) : $.slice(K, G);
  return V.push(A.trim()), V.map(function(q) {
    var H = q.indexOf("\\|") !== -1 ? q.replace(/\\\|/g, "|") : q;
    return H ? Q9(H, false, Q, Z) : [];
  });
}
function U4($, Q, Z, Y) {
  let G = K0($, Q);
  var V = $.indexOf("|", Q);
  if (V < 0 || V >= G) return null;
  let K = s($, G);
  if (K >= $.length) return null;
  let X = K0($, K);
  if (!j4($, K, X)) return null;
  let J = $.slice(Q, G), F = $.slice(K, X);
  for (var U = [], W = 0, O = F.length; W < O && (F.charCodeAt(W) === N || F.charCodeAt(W) === b); ) W++;
  for (W < O && F.charCodeAt(W) === S0 && W++; W < O; ) {
    for (; W < O && (F.charCodeAt(W) === N || F.charCodeAt(W) === b); ) W++;
    if (W >= O || F.charCodeAt(W) === S0) break;
    var z = F.charCodeAt(W) === g0;
    for (z && W++; W < O && F.charCodeAt(W) === d; ) W++;
    var B2 = W < O && F.charCodeAt(W) === g0;
    for (B2 && W++, U.push(z && B2 ? "center" : B2 ? "right" : z ? "left" : null); W < O && (F.charCodeAt(W) === N || F.charCodeAt(W) === b); ) W++;
    W < O && F.charCodeAt(W) === S0 && W++;
  }
  let A = F4(J, Z, Y);
  if (U.length !== A.length) return null;
  let q = [], H = s($, X);
  for (; H < $.length; ) {
    let w = K0($, H), L = $.slice(H, w);
    if (a0($, H, w)) break;
    if (P0($, H, w), V0 < 4) {
      var j2 = $.charCodeAt(H + $0);
      if (j2 === o || j2 === G5 || (j2 === d || j2 === A0 || j2 === q0) && C5($, H)) break;
      if (j2 === e || j2 === N0) {
        for (var M = H + $0, y = 0; M < w && $.charCodeAt(M) === j2; ) y++, M++;
        if (y >= 3) break;
      }
    }
    q.push(F4(L, Z, Y)), H = s($, w);
  }
  if (Y.optimizeForStreaming && q.length === 0) return null;
  for (var v = A.length, D = 0; D < q.length; D++) if (q[D].length < v) for (; q[D].length < v; ) q[D].push([]);
  else q[D].length > v && (q[D].length = v);
  return { node: { type: _.table, header: A, cells: q, align: U }, end: H };
}
function A6($, Q, Z) {
  var Y = K0($, Q);
  if (P0($, Q, Y), V0 > 3) return null;
  var G = Q + $0;
  if ($.charCodeAt(G) !== z0) return null;
  if (G + 1 < $.length && $.charCodeAt(G + 1) === F5) {
    var V = j6($, G, Z);
    return V || null;
  }
  Z.refs || (Z.refs = {});
  var K = M4($, G, Z.refs);
  return K === null ? null : { node: { type: _.refCollection }, end: K };
}
function j6($, Q, Z) {
  var Y = $.length;
  if ($.charCodeAt(Q) !== z0 || Q + 1 >= Y || $.charCodeAt(Q + 1) !== F5) return null;
  for (var G = Q + 2, V = G; G < Y && $.charCodeAt(G) !== K5; ) {
    if ($.charCodeAt(G) === f) return null;
    G++;
  }
  if (G >= Y) return null;
  var K = ("^" + $.slice(V, G)).toLowerCase();
  if (G++, G >= Y || $.charCodeAt(G) !== g0) return null;
  for (G++; G < Y && ($.charCodeAt(G) === N || $.charCodeAt(G) === b); ) G++;
  if (G < Y && $.charCodeAt(G) === f) for (G++; G < Y && ($.charCodeAt(G) === N || $.charCodeAt(G) === b); ) G++;
  var X = $.indexOf(`
`, G);
  X < 0 && (X = Y);
  for (var J = $.slice(G, X).trim(), F = X < Y ? X + 1 : Y; F < Y; ) {
    var U = K0($, F);
    if (P0($, F, U), V0 >= 2 && !a0($, F, U)) J += `
` + $.slice(F, U), F = s($, U);
    else if (a0($, F, U)) {
      var W = s($, U);
      if (W < Y) {
        var O = K0($, W);
        if (P0($, W, O), V0 >= 2) {
          J += `
`, F = s($, U);
          continue;
        }
      }
      break;
    } else break;
  }
  var z = Z.refs;
  return z && !z[K] && (z[K] = { target: J, title: void 0 }), { node: { type: _.footnote }, end: F };
}
function M6($, Q, Z, Y) {
  let G = Q, V = 0, K = 0, X = -1;
  for (; G < $.length; ) {
    let u = X >= 0 ? X : K0($, G);
    if (X = -1, a0($, G, u)) break;
    if (P0($, G, u), V0 < 4 && K > 0 && !Z.g) {
      let m = $.charCodeAt(G + $0);
      if (m === V5 || m === d) {
        let T = G + $0;
        for (; T < u && $.charCodeAt(T) === m; ) T++;
        for (; T < u && ($.charCodeAt(T) === N || $.charCodeAt(T) === b); ) T++;
        if (T >= u) {
          V = m === V5 ? 1 : 2, G = s($, u);
          break;
        }
      }
    }
    K = u;
    let E = s($, u);
    if (E < $.length) {
      if ($.charCodeAt(E) === L9) {
        var J = K0($, E);
        G = s($, J), K = J;
        continue;
      }
      let m = K0($, E);
      if (X = m, P0($, E, m), V0 < 4) {
        let T = $.charCodeAt(E + $0);
        if (T === o) {
          G = E;
          break;
        }
        if (T === G5) {
          for (var F = E + $0, U = 0; F < m && $.charCodeAt(F) === G5 && U <= 6; ) U++, F++;
          if (U >= 1 && U <= 6 && (F >= m || $.charCodeAt(F) === N || $.charCodeAt(F) === b)) {
            G = E;
            break;
          }
        }
        if (T === e || T === N0) {
          for (var W = E + $0, O = 0; W < m && $.charCodeAt(W) === T; ) O++, W++;
          if (O >= 3) {
            G = E;
            break;
          }
        }
        if (T === Q0) {
          var z = E + $0 + 1, B2 = z < m ? $.charCodeAt(z) : 0, A = B2 === p0 || B2 === s5;
          if (!A && B2 === H0) {
            for (var q = z + 1, H = q; H < m && ($.charCodeAt(H) >= p && $.charCodeAt(H) <= c || $.charCodeAt(H) >= G0 && $.charCodeAt(H) <= U0 || $.charCodeAt(H) >= r && $.charCodeAt(H) <= i || $.charCodeAt(H) === d); ) H++;
            H > q && (A = x9.has($.slice(q, H).toLowerCase()));
          } else if (!A) {
            for (var j2 = z; j2 < m && ($.charCodeAt(j2) >= p && $.charCodeAt(j2) <= c || $.charCodeAt(j2) >= G0 && $.charCodeAt(j2) <= U0 || $.charCodeAt(j2) >= r && $.charCodeAt(j2) <= i || $.charCodeAt(j2) === d); ) j2++;
            if (j2 > z) {
              var M = $.slice(z, j2).toLowerCase();
              A = x9.has(M) || B9.has(M);
            }
          }
          if (A && I4($, E, Z, Y)) {
            G = E;
            break;
          }
        }
        if (T === d || T === A0 || T === b5) {
          let S = E + $0 + 1;
          if (S < m && ($.charCodeAt(S) === N || $.charCodeAt(S) === b)) {
            var y = H9($, S, m);
            if (y < m && !C5($, E)) {
              G = E;
              break;
            }
          }
        }
        if (T >= r && T <= i) {
          let S = E + $0;
          for (; S < m && $.charCodeAt(S) >= r && $.charCodeAt(S) <= i; ) S++;
          if (S < m && ($.charCodeAt(S) === s0 || $.charCodeAt(S) === q5) && S - (E + $0) === 1 && $.charCodeAt(E + $0) === 49) {
            var v = S + 1;
            if (v < m && ($.charCodeAt(v) === N || $.charCodeAt(v) === b)) {
              var D = H9($, v, m);
              if (D < m) {
                G = E;
                break;
              }
            }
          }
        }
        if (T === S0) {
          let S = s($, m);
          if (S < $.length) {
            let a = K0($, S);
            if (j4($, S, a)) {
              G = E;
              break;
            }
          }
        }
        if ((T === d || T === A0 || T === q0) && C5($, E)) {
          if (T !== d) {
            G = E;
            break;
          }
          let S = 0, a = E + $0;
          for (; a < m && $.charCodeAt(a) === d; ) S++, a++;
          for (; a < m && ($.charCodeAt(a) === N || $.charCodeAt(a) === b); ) a++;
          if (a < m) {
            G = E;
            break;
          }
        }
      }
    }
    G = s($, u);
  }
  for (var w = V ? K : G; w > Q && ($.charCodeAt(w - 1) === f || $.charCodeAt(w - 1) === N5 || $.charCodeAt(w - 1) === N || $.charCodeAt(w - 1) === b); ) w--;
  for (var L = Q; L < w && ($.charCodeAt(L) === N || $.charCodeAt(L) === b); ) L++;
  if (L >= w) return null;
  for (var R = false, I = L; I < w; I++) if ($.charCodeAt(I) === L9) {
    R = true;
    break;
  }
  var x = R ? $.slice(L, w).replace(/\u001E/g, "") : $.slice(L, w);
  if (!x) return null;
  let k = Q9(x, true, Z, Y);
  if (V) {
    let E = ((Y == null ? void 0 : Y.slugify) || f0)(x);
    return { node: { type: _.heading, level: V, children: k, id: E }, end: G };
  }
  return { node: { type: _.paragraph, children: k }, end: G };
}
function Q9($, Q, Z, Y) {
  var G = Z.m;
  if (!G) {
    var V = Z.f;
    Z.f = Q;
    var K = _5($, 0, $.length, Z, Y);
    return Z.f = V, K;
  }
  var X = [];
  return G.push({ dest: X, text: $, breaks: Q, inline: Z.inline, inAnchor: Z.inAnchor, inHTML: Z.inHTML, htmlDepth: Z.c, inList: Z.inList, inBlockQuote: Z.inBlockQuote, noSetext: Z.g, depth: Z.j }), X;
}
function _6($, Q, Z) {
  if ($.charCodeAt(Q) !== e) return null;
  let Y = h5($, Q, Z, 96), G = Q + Y;
  for (; G < Z; ) {
    let V = $.indexOf("`", G);
    if (V < 0 || V >= Z) return null;
    let K = h5($, V, Z, 96);
    if (K === Y) {
      let X = $.slice(Q + Y, V);
      return X.indexOf(`
`) !== -1 && (X = X.replace(/\n/g, " ")), X.length > 0 && X[0] === " " && X[X.length - 1] === " " && X.trim().length > 0 && (X = X.slice(1, -1)), { node: { type: _.codeInline, text: X }, end: V + K };
    }
    G = V + K;
  }
  return null;
}
function J$($, Q, Z) {
  if ($.charCodeAt(Q) !== e) return Q;
  let Y = h5($, Q, Z, 96), G = Q + Y;
  for (; G < Z; ) {
    let V = $.indexOf("`", G);
    if (V < 0 || V >= Z) return Q;
    let K = h5($, V, Z, 96);
    if (K === Y) return V + K;
    G = V + K;
  }
  return Q;
}
function I6($, Q, Z) {
  if ($.charCodeAt(Q) !== Q0) return Q;
  if (Q + 1 < Z && $.charCodeAt(Q + 1) === H0) {
    let F = Q + 2;
    for (; F < Z && $.charCodeAt(F) !== o; ) F++;
    return F < Z ? F + 1 : Q;
  }
  if (Q + 3 < Z && $.charCodeAt(Q + 1) === p0 && $.charCodeAt(Q + 2) === d && $.charCodeAt(Q + 3) === d) {
    let F = $.indexOf("-->", Q + 4);
    return F >= 0 ? F + 3 : Q;
  }
  let Y = Q + 1, G = Y;
  for (; Y < Z; ) {
    let F = $.charCodeAt(Y);
    if (F >= p && F <= c || F >= G0 && F <= U0 || F >= r && F <= i || F === d) Y++;
    else break;
  }
  if (Y === G) return Q;
  let V = $.slice(G, Y).toLowerCase(), K = false;
  for (; Y < Z; ) {
    let F = $.charCodeAt(Y);
    if (F === o) {
      Y++;
      break;
    }
    if (F === H0 && Y + 1 < Z && $.charCodeAt(Y + 1) === o) {
      Y += 2, K = true;
      break;
    }
    if (F === w5 || F === E5) {
      var X = F;
      for (Y++; Y < Z && $.charCodeAt(Y) !== X; ) Y++;
      Y < Z && Y++;
      continue;
    }
    if (F === f) return Q;
    Y++;
  }
  if (K || r5(V)) return Y;
  let J = 1;
  for (; Y < Z && J > 0; ) if ($.charCodeAt(Y) === Q0) if (Y + 1 < Z && $.charCodeAt(Y + 1) === H0) {
    let F = Y + 2, U = F;
    for (; U < Z && ($.charCodeAt(U) >= p && $.charCodeAt(U) <= c || $.charCodeAt(U) >= G0 && $.charCodeAt(U) <= U0); ) U++;
    if ($.slice(F, U).toLowerCase() === V) {
      for (; U < Z && $.charCodeAt(U) !== o; ) U++;
      if (U < Z && U++, J--, J === 0) return U;
    }
    Y = U;
  } else {
    let F = Y + 1, U = F;
    for (; U < Z && ($.charCodeAt(U) >= p && $.charCodeAt(U) <= c || $.charCodeAt(U) >= G0 && $.charCodeAt(U) <= U0); ) U++;
    $.slice(F, U).toLowerCase() === V && J++, Y++;
  }
  else Y++;
  return Y;
}
function v6($, Q, Z, Y, G) {
  if ($.charCodeAt(Q) !== N0 || Q + 1 >= Z || $.charCodeAt(Q + 1) !== N0) return null;
  let V = Q + 2;
  for (; V + 1 < Z; ) {
    let K = $.charCodeAt(V);
    if (K === e) {
      let X = J$($, V, Z);
      if (X > V) {
        V = X;
        continue;
      }
    }
    if (K === N0 && $.charCodeAt(V + 1) === N0) {
      let X = $.slice(Q + 2, V), J = _5(X, 0, X.length, Y, G);
      return { node: { type: _.textFormatted, tag: "del", children: J }, end: V + 2 };
    }
    K === b0 && V + 1 < Z && V++, V++;
  }
  return null;
}
function N6($, Q, Z, Y, G) {
  if ($.charCodeAt(Q) !== V5 || Q + 1 >= Z || $.charCodeAt(Q + 1) !== V5) return null;
  let V = Q + 2;
  for (; V + 1 < Z; ) {
    let K = $.charCodeAt(V);
    if (K === e) {
      let X = J$($, V, Z);
      if (X > V) {
        V = X;
        continue;
      }
    }
    if (K === V5 && $.charCodeAt(V + 1) === V5 && V > Q + 2) {
      let X = $.slice(Q + 2, V), J = _5(X, 0, X.length, Y, G);
      return { node: { type: _.textFormatted, tag: "mark", children: J }, end: V + 2 };
    }
    K === b0 && V + 1 < Z && V++, V++;
  }
  return null;
}
function q4($, Q, Z) {
  return $ < t5 ? !!(X$($) & c5) : m$.test(Q[Z]);
}
function W4($, Q, Z) {
  return $ < t5 ? !!(X$($) & T5) : h$.test(Q[Z]);
}
function P6($, Q, Z) {
  var Y = $.charCodeAt(Q);
  if (Y !== A0 && Y !== q0) return null;
  var G = h5($, Q, Z, Y);
  if (G === 0) return null;
  var V = Q > 0 ? $.charCodeAt(Q - 1) : 32, K = Q + G < Z ? $.charCodeAt(Q + G) : 32, X = W4(V, $, Q - 1), J = W4(K, $, Q + G), F = Q > 0 ? q4(V, $, Q - 1) : false, U = Q + G < Z ? q4(K, $, Q + G) : false, W = !J && (!U || X || F), O = !X && (!F || J || U), z, B2;
  return Y === A0 ? (z = W, B2 = O) : (z = W && (!O || F), B2 = O && (!W || U)), { len: G, canOpen: z, canClose: B2 };
}
function w6($, Q, Z, Y) {
  if (Q.length !== 0) {
    for (var G = $.length, V = Array(G), K = null, X = null, J = 0; J < G; J++) {
      var F = { node: $[J], prev: X, next: null };
      X ? X.next = F : K = F, V[J] = F, X = F;
    }
    for (var U = Array(Q.length), W = 0; W < Q.length; W++) U[W] = V[Q[W].idx];
    for (var O = [], z = 0; z < 12; z++) O[z] = -1;
    for (var B2 = 0; B2 < Q.length; ) {
      var A = Q[B2];
      if (!A.active || !A.canClose) {
        B2++;
        continue;
      }
      for (var q = A.ch === A0 ? 0 : 1, H = q * 6 + A.len % 3 * 2 + (A.canOpen ? 1 : 0), j2 = O[H] !== void 0 ? O[H] : -1, M = -1, y = B2 - 1; y > j2; y--) {
        var v = Q[y];
        if (!(!v.active || v.ch !== A.ch || !v.canOpen) && !((A.canOpen || v.canClose) && (v.len + A.len) % 3 === 0 && v.len % 3 !== 0)) {
          M = y;
          break;
        }
      }
      if (M < 0) {
        O[H] = B2 - 1, !A.canOpen && (A.active = false), B2++;
        continue;
      }
      var D = Q[M], w = D.len >= 2 && A.len >= 2, L = w ? 2 : 1;
      D.len -= L, A.len -= L;
      var R = U[M], I = U[B2], x = R.node, k = I.node;
      x.text = x.text.slice(0, x.text.length - L), k.text = k.text.slice(L);
      for (var u = [], E = R.next; E && E !== I; ) u.push(E.node), E = E.next;
      var m = { type: _.textFormatted, tag: w ? "strong" : "em", children: u }, T = { node: m, prev: R, next: I };
      R.next = T, I.prev = T;
      for (var S = M + 1; S < B2; S++) Q[S].active = false;
      if (D.len === 0 && (D.active = false, x.text === "")) {
        var a = R.prev;
        T.prev = a, a ? a.next = T : K = T;
      }
      if (A.len === 0) {
        if (A.active = false, k.text === "") {
          var g = I.next;
          T.next = g, g && (g.prev = T);
        }
      } else continue;
      B2++;
    }
    for (var X0 = 0, n = K; n; ) {
      var y0 = n.node;
      if (y0.type === _.text) {
        var C0 = y0;
        if (C0.text === "") {
          n = n.next;
          continue;
        }
        if (X0 > 0 && $[X0 - 1].type === _.text) {
          $[X0 - 1].text += C0.text, n = n.next;
          continue;
        }
      }
      $[X0++] = y0, n = n.next;
    }
    $.length = X0;
  }
}
function z4($, Q, Z, Y, G) {
  let V = $.charCodeAt(Q) === p0, K = V ? Q + 1 : Q;
  if ($.charCodeAt(K) !== z0) return null;
  var X = $.indexOf("]", K + 1);
  if (X < 0 || X >= Z) return null;
  for (var J = K + 1, F = -1, U = 1; J < Z && U > 0; ) {
    var W = $.charCodeAt(J);
    if (W === b0 && J + 1 < Z) {
      J += 2;
      continue;
    }
    if (W === e) {
      var O = J$($, J, Z);
      if (O > J) {
        J = O;
        continue;
      }
    }
    if (W === Q0) {
      var z = v4($, J, Z);
      if (z) {
        J = z.end;
        continue;
      }
      var B2 = I6($, J, Z);
      if (B2 > J) {
        J = B2;
        continue;
      }
    }
    W === z0 ? U++ : W === K5 && U--, J++;
  }
  if (U !== 0) return null;
  var A = J - 1, q = $.slice(K + 1, A), H = J < Z ? $.charCodeAt(J) : 0, j2 = false;
  if (H === U5) {
    var M = true;
    for (J++; J < Z && ($.charCodeAt(J) === N || $.charCodeAt(J) === f); ) J++;
    var y = "", v = J;
    if (J < Z && $.charCodeAt(J) === Q0) {
      for (J++, v = J; v < Z && $.charCodeAt(v) !== o; ) {
        if ($.charCodeAt(v) === b0 && v + 1 < Z) {
          v += 2;
          continue;
        }
        if ($.charCodeAt(v) === f) {
          M = false;
          break;
        }
        v++;
      }
      M && (v >= Z || $.charCodeAt(v) !== o) && (M = false), M && (y = $.slice(J, v), v++);
    } else if (M) if (Y.h !== void 0 && J >= Y.h) M = false;
    else {
      for (var D = 0, w = false; v < Z; ) {
        var L = $.charCodeAt(v);
        if (L === b0 && v + 1 < Z) {
          v += 2;
          continue;
        }
        if (L === U5) D++;
        else if (L === q5) {
          if (w = true, D === 0) break;
          D--;
        } else if (L === N || L === f) break;
        v++;
      }
      v >= Z && !w && (Y.h === void 0 || J < Y.h) && (Y.h = J), y = $.slice(J, v);
    }
    if (M) {
      for (J = v; J < Z && ($.charCodeAt(J) === N || $.charCodeAt(J) === f); ) J++;
      var R;
      if (J < Z) {
        var I = $.charCodeAt(J);
        if (I === w5 || I === E5 || I === U5) {
          var x = I === U5 ? 41 : I;
          J++;
          for (var k = J; J < Z && $.charCodeAt(J) !== x; ) $.charCodeAt(J) === b0 && J + 1 < Z && J++, J++;
          J >= Z ? M = false : (R = $.slice(k, J), J++);
        }
      }
      if (M) {
        for (; J < Z && ($.charCodeAt(J) === N || $.charCodeAt(J) === f); ) J++;
        (J >= Z || $.charCodeAt(J) !== q5) && (M = false);
      }
    }
    if (M) {
      J++, y = Z9(y), R !== void 0 && (R = o5(Z9(R)));
      var u = (G == null ? void 0 : G.sanitizer) || j5, E = u(y, V ? "img" : "a", V ? "src" : "href"), m = E === null ? null : y;
      if (V) {
        var T = _5(q, 0, q.length, Y, G), S = K$(T);
        return { node: { type: _.image, target: m, alt: S, title: R }, end: J };
      } else {
        var a = Y.inAnchor;
        Y.inAnchor = true;
        var g = a ? [{ type: _.text, text: q }] : _5(q, 0, q.length, Y, G);
        return Y.inAnchor = a, !Y.inAnchor && V$(g) ? null : { node: { type: _.link, target: m, title: R, children: g }, end: J };
      }
    } else J = A + 1, j2 = true;
  }
  var X0 = "", n = J;
  if (!j2 && H === z0) {
    var y0 = J + 1;
    n = y0;
    for (var C0 = false; n < Z && $.charCodeAt(n) !== K5; ) {
      if ($.charCodeAt(n) === b0 && n + 1 < Z) {
        n += 2;
        continue;
      }
      if ($.charCodeAt(n) === z0) {
        C0 = true;
        break;
      }
      n++;
    }
    if (C0 || n >= Z) return null;
    var m0 = $.slice(y0, n);
    if (m0.trim()) X0 = E9(m0);
    else {
      if (Z4(q)) return null;
      X0 = E9(q);
    }
    n = n + 1;
  } else {
    if (Z4(q)) return null;
    X0 = E9(q);
  }
  var t = Y.refs && Y.refs[X0];
  if (!t) return null;
  if (V) return { node: { type: _.image, target: t.target, alt: K$(_5(q, 0, q.length, Y, G)), title: t.title }, end: n };
  var l = Y.inAnchor;
  Y.inAnchor = true;
  var g = l ? [{ type: _.text, text: q }] : _5(q, 0, q.length, Y, G);
  return Y.inAnchor = l, !Y.inAnchor && V$(g) ? null : { node: { type: _.link, target: t.target, title: t.title, children: g }, end: n };
}
function v4($, Q, Z) {
  if ($.charCodeAt(Q) !== Q0) return null;
  for (var Y = Q + 1; Y < Z; ) {
    var G = $.charCodeAt(Y);
    if (G === o) break;
    if (G === N || G === f || G === N5 || G === Q0) return null;
    Y++;
  }
  if (Y >= Z || $.charCodeAt(Y) !== o) return null;
  var V = $.slice(Q + 1, Y), K = V.match(/^([a-zA-Z][a-zA-Z0-9+.-]{1,31}):([^\x00-\x20]*)$/);
  return K ? { node: { type: _.link, target: V, title: void 0, children: [{ type: _.text, text: V }] }, end: Y + 1 } : V.indexOf("@") !== -1 && /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/.test(V) ? { node: { type: _.link, target: "mailto:" + V, title: void 0, children: [{ type: _.text, text: V }] }, end: Y + 1 } : null;
}
function b6($, Q, Z, Y) {
  if ($.charCodeAt(Q) !== z0 || Q + 1 >= Z || $.charCodeAt(Q + 1) !== F5) return null;
  let G = Q + 2;
  for (; G < Z && $.charCodeAt(G) !== K5 && $.charCodeAt(G) !== f; ) G++;
  if (G >= Z || $.charCodeAt(G) !== K5) return null;
  let V = $.slice(Q + 2, G);
  return V ? { node: { type: _.footnoteReference, target: "#" + f0(V), text: V }, end: G + 1 } : null;
}
function y6($, Q, Z, Y) {
  if (Y.disableBareUrls) return null;
  var G = "", V = false, K = $.charCodeAt(Q);
  if (K === q9 || K === 72 ? Q + 8 <= Z && $.charCodeAt(Q + 1) === p5 && $.charCodeAt(Q + 2) === p5 && $.charCodeAt(Q + 3) === y9 && ($.charCodeAt(Q + 4) === c9 && $.charCodeAt(Q + 5) === g0 && $.charCodeAt(Q + 6) === H0 && $.charCodeAt(Q + 7) === H0 ? G = "https://" : $.charCodeAt(Q + 4) === g0 && $.charCodeAt(Q + 5) === H0 && $.charCodeAt(Q + 6) === H0 && (G = "http://")) : K === P5 || K === 70 ? Q + 6 <= Z && $.charCodeAt(Q + 1) === p5 && $.charCodeAt(Q + 2) === y9 && $.charCodeAt(Q + 3) === g0 && $.charCodeAt(Q + 4) === H0 && $.charCodeAt(Q + 5) === H0 && (G = "ftp://") : (K === S5 || K === 87) && Q + 4 <= Z && $.charCodeAt(Q + 1) === S5 && $.charCodeAt(Q + 2) === S5 && $.charCodeAt(Q + 3) === s0 && (G = "www.", V = true), !G) return null;
  let X = Q + G.length;
  for (; X < Z; ) {
    let D = $.charCodeAt(X);
    if (D === N || D === f || D === b || D === N5 || D === Q0 || D === o) break;
    X++;
  }
  for (var J = 0, F = 0, U = Q; U < X; U++) {
    var W = $.charCodeAt(U);
    W === U5 ? J++ : W === q5 && F++;
  }
  let O = X;
  for (; O > Q + G.length; ) {
    let D = $.charCodeAt(O - 1);
    if (D === s0 || D === w$ || D === g0 || D === p0 || D === s5 || D === q5 || D === A0 || D === q0 || D === N0) {
      if (D === q5) {
        if (J >= F) break;
        F--;
      }
      O--;
    } else if (D === W9) {
      for (var z = O - 2; z > Q && ($.charCodeAt(z) >= p && $.charCodeAt(z) <= c || $.charCodeAt(z) >= G0 && $.charCodeAt(z) <= U0 || $.charCodeAt(z) >= r && $.charCodeAt(z) <= i); ) z--;
      z >= Q && $.charCodeAt(z) === F9 ? O = z : O--;
    } else break;
  }
  if (O <= Q + G.length) return null;
  var B2 = Q + (V ? 4 : G.length), A = $.indexOf("/", B2);
  if ((A < 0 || A > O) && (A = O), V && $.indexOf(".", B2) === -1) return null;
  for (var q = -1, H = -1, j2 = A - 1; j2 >= B2; j2--) if ($.charCodeAt(j2) === s0) if (q < 0) q = j2;
  else {
    H = j2;
    break;
  }
  for (var M = H >= 0 ? H + 1 : B2, j2 = M; j2 < A; j2++) if ($.charCodeAt(j2) === q0) return null;
  var y = $.slice(Q, O), v = V ? "http://" + y : y;
  return { node: { type: _.link, target: v, title: void 0, children: [{ type: _.text, text: y }] }, end: O };
}
function R6($, Q, Z, Y) {
  if (Y.disableBareUrls) return null;
  for (var G = Q, V = G; G < Z; ) {
    var K = $.charCodeAt(G);
    if (K >= p && K <= c || K >= G0 && K <= U0 || K >= r && K <= i || K === s0 || K === p0 || K === G5 || K === 36 || K === J9 || K === F9 || K === E5 || K === A0 || K === b5 || K === H0 || K === V5 || K === s5 || K === F5 || K === q0 || K === e || K === y5 || K === S0 || K === R9 || K === N0 || K === d) G++;
    else break;
  }
  if (G === V || G >= Z || $.charCodeAt(G) !== b9) return null;
  G++;
  for (var X = G, J = -1, F = G; G < Z; ) {
    var K = $.charCodeAt(G);
    if (K >= p && K <= c || K >= G0 && K <= U0 || K >= r && K <= i) G++;
    else if ((K === d || K === q0) && G > X) G++;
    else if (K === s0) {
      if (G === X) break;
      var U = $.charCodeAt(G - 1);
      if (U === d || U === q0 || G - F > 63) break;
      if (G + 1 < Z) {
        var W = $.charCodeAt(G + 1);
        if (W >= p && W <= c || W >= G0 && W <= U0 || W >= r && W <= i) J = G, F = G + 1, G++;
        else break;
      } else break;
    } else break;
  }
  if (G - F > 63 || J < 0) return null;
  var O = $.charCodeAt(G - 1);
  if (!(O >= p && O <= c || O >= G0 && O <= U0 || O >= r && O <= i) || G <= J + 1) return null;
  for (var z = -1, B2 = J - 1; B2 >= X; B2--) if ($.charCodeAt(B2) === s0) {
    z = B2;
    break;
  }
  for (var A = z >= 0 ? z + 1 : X, B2 = A; B2 < G; B2++) if ($.charCodeAt(B2) === q0) return null;
  var q = $.slice(Q, G);
  return { node: { type: _.link, target: "mailto:" + q, title: void 0, children: [{ type: _.text, text: q }] }, end: G };
}
function V$($) {
  for (var Q = 0; Q < $.length; Q++) if ($[Q].type === _.link || "children" in $[Q] && Array.isArray($[Q].children) && V$($[Q].children)) return true;
  return false;
}
function K$($) {
  for (var Q = "", Z = 0; Z < $.length; Z++) {
    var Y = $[Z];
    Y.type === _.text ? Q += Y.text : Y.type === _.breakLine ? Q += " " : Y.type === _.codeInline ? Q += Y.text : "children" in Y && Array.isArray(Y.children) ? Q += K$(Y.children) : Y.type === _.image && (Q += Y.alt || "");
  }
  return Q;
}
function i5($) {
  return { type: _.text, text: $ };
}
function L6($, Q, Z) {
  var Y = Q + 1;
  if (Y >= Z) return -1;
  if ($.charCodeAt(Y) === G5) {
    Y++;
    var G = Y < Z && ($.charCodeAt(Y) === l9 || $.charCodeAt(Y) === o9);
    G && Y++;
    for (var V = Y, K = G ? 6 : 7; Y < Z && Y - V <= K; ) {
      var X = $.charCodeAt(Y), J = X >= r && X <= i || G && (X >= p && X <= U9 || X >= G0 && X <= P5);
      if (!J) break;
      Y++;
    }
    return Y === V || Y - V > K ? -1 : Y < Z && $.charCodeAt(Y) === W9 ? Y + 1 : -1;
  }
  for (var F = Y; Y < Z && Y - F < 48; ) {
    var U = $.charCodeAt(Y);
    if (U >= p && U <= c || U >= G0 && U <= U0 || U >= r && U <= i) {
      Y++;
      continue;
    }
    break;
  }
  return Y === F ? -1 : Y < Z && $.charCodeAt(Y) === W9 ? Y + 1 : -1;
}
function k6($, Q, Z, Y, G) {
  if ($.charCodeAt(Q) !== Q0) return null;
  var V = Q + 1;
  if (V >= Z) return null;
  var K = $.charCodeAt(V);
  if (K === p0 && V + 1 < Z && $.charCodeAt(V + 1) === d && V + 2 < Z && $.charCodeAt(V + 2) === d) {
    var X = V + 3;
    if (X < Z && $.charCodeAt(X) === o) return { node: { type: _.htmlComment, text: "", l: true }, end: X + 1 };
    if (X + 1 < Z && $.charCodeAt(X) === d && $.charCodeAt(X + 1) === o) return { node: { type: _.htmlComment, text: "-", l: true }, end: X + 2 };
    var J = $.indexOf("-->", X);
    return J !== -1 && J <= Z - 3 ? { node: { type: _.htmlComment, text: $.slice(Q + 4, J), l: false }, end: J + 3 } : null;
  }
  if (K === s5) {
    var F = $.indexOf("?>", V + 1);
    return F !== -1 && F < Z ? { node: { type: _.htmlSelfClosing, tag: "?", attrs: {}, a: $.slice(Q, F + 2), b: false }, end: F + 2 } : null;
  }
  if (K === p0 && V + 1 < Z) {
    var U = $.charCodeAt(V + 1);
    if (U === z0 && $.slice(V + 1, V + 8) === "[CDATA[") {
      var W = $.indexOf("]]>", V + 8);
      return W !== -1 && W < Z ? { node: { type: _.htmlSelfClosing, tag: "![CDATA[", attrs: {}, a: $.slice(Q, W + 3), b: false }, end: W + 3 } : null;
    }
    if (U >= p && U <= c) {
      var O = $.indexOf(">", V + 2);
      return O !== -1 && O < Z ? { node: { type: _.htmlSelfClosing, tag: "!" + $.slice(V + 1, O), attrs: {}, a: $.slice(Q, O + 1), b: false }, end: O + 1 } : null;
    }
  }
  if (K === H0) {
    var z = V + 1;
    if (z >= Z) return null;
    var B2 = $.charCodeAt(z);
    if (!(B2 >= p && B2 <= c || B2 >= G0 && B2 <= U0)) return null;
    for (z++; z < Z; ) {
      var A = $.charCodeAt(z);
      if (A >= p && A <= c || A >= G0 && A <= U0 || A >= r && A <= i || A === d) z++;
      else break;
    }
    for (; z < Z && ($.charCodeAt(z) === N || $.charCodeAt(z) === b || $.charCodeAt(z) === f); ) z++;
    if (z < Z && $.charCodeAt(z) === o) {
      var q = $.slice(V + 1, z).trim();
      return { node: { type: _.htmlSelfClosing, tag: q, attrs: {}, a: $.slice(Q, z + 1), b: true }, end: z + 1 };
    }
    return null;
  }
  if (!(K >= p && K <= c || K >= G0 && K <= U0)) return null;
  var H = M5($, Q);
  if (!H) return null;
  var j2 = H.tag, M = j2.toLowerCase(), y = H.selfClosing;
  if (y || r5(j2)) return { node: { type: _.htmlSelfClosing, tag: j2, attrs: L5(H.attrs, j2, G), a: H.sanitized ? Z$(H) : $.slice(Q, H.end), b: false }, end: H.end };
  var v = B9.has(M), D = G$($.slice(0, Z), H.end, j2);
  if (D === -1) return { node: { type: _.htmlSelfClosing, tag: j2, attrs: L5(H.attrs, j2, G), a: H.sanitized ? Z$(H) : $.slice(Q, H.end), b: false }, end: H.end };
  var w = B6($, "</" + M, D), L = $.slice(H.end, w), R = [];
  if (v) L.trim() && (R = [{ type: _.text, text: L }]);
  else {
    var I = L.trim();
    if (I) {
      var { inAnchor: x, inline: k, f: u } = Y;
      M === "a" && (Y.inAnchor = true), Y.f = false;
      var E = I.indexOf(`

`) !== -1 || /^#{1,6}\s/.test(I);
      E ? (Y.inline = false, R = m5(I, Y, G)) : R = _5(I, 0, I.length, Y, G), Y.inAnchor = x, Y.inline = k, Y.f = u;
    }
  }
  return { node: { type: _.htmlBlock, tag: j2, attrs: L5(H.attrs, j2, G), i: H.rawAttrs, children: R, a: void 0, text: g5(L), d: false, b: false }, end: D };
}
var S6 = 200;
var O9 = 0;
function _5($, Q, Z, Y, G) {
  if (O9++, O9 > S6) return O9--, [{ type: _.text, text: $.slice(Q, Z) }];
  var V = Y.h;
  Y.h = void 0;
  let K = Y;
  if (G.optimizeForStreaming) {
    let B5 = function(D0, k0) {
      for (var Q5 = 1, x0 = k0 + 1; x0 < D0.length; x0++) {
        var Y5 = D0.charCodeAt(x0);
        if (Y5 === z0) Q5++;
        else if (Y5 === K5 && (Q5--, Q5 === 0)) return x0;
      }
      return -1;
    }, P = $.slice(Q, Z), F0 = P;
    for (var X = 0, J = 0, F = 0, U = 0, W = 0, O = -1, z = -1, B2 = -1, A = -1, q = -1, H = 0; H < P.length; H++) {
      var j2 = P.charCodeAt(H);
      j2 === A0 ? H + 1 < P.length && P.charCodeAt(H + 1) === A0 ? (X++, O = H, H++) : (J++, z = H) : j2 === q0 ? H + 1 < P.length && P.charCodeAt(H + 1) === q0 ? (F++, B2 = H, H++) : (U++, A = H) : j2 === N0 && H + 1 < P.length && P.charCodeAt(H + 1) === N0 && (W++, q = H, H++);
    }
    var M = [];
    W % 2 === 1 && q >= 0 && M.push([q, 2]), F % 2 === 1 && B2 >= 0 && M.push([B2, 2]), U % 2 === 1 && A >= 0 && M.push([A, 1]), X % 2 === 1 && O >= 0 && M.push([O, 2]), J % 2 === 1 && z >= 0 && M.push([z, 1]), M.sort(function(D0, k0) {
      return k0[0] - D0[0];
    });
    for (var y = 0; y < M.length; y++) {
      var v = M[y][0], D = M[y][1];
      P = P.slice(0, v) + P.slice(v + D);
    }
    let h0 = 0, Z5 = -1;
    for (let D0 = 0; D0 < P.length; D0++) P.charCodeAt(D0) === e && (h0++, Z5 = D0);
    if (h0 % 2 === 1 && Z5 !== -1) {
      let D0 = false, k0 = -1, Q5 = 0;
      for (; Q5 < P.length; ) P.charCodeAt(Q5) === e && (D0 ? (D0 = false, k0 = -1) : (k0 = Q5, D0 = true)), Q5++;
      D0 && k0 !== -1 && (P = P.slice(0, k0));
    }
    for (var w = true; w; ) {
      w = false;
      for (var L = -1, R = -1, I = -1, x = false, k = 0; k < P.length; k++) {
        var u = P.charCodeAt(k);
        if (u === z0 && (k === 0 || P.charCodeAt(k - 1) !== b0)) {
          var E = k > 0 && P.charCodeAt(k - 1) === p0, m = E ? k - 1 : k, T = B5(P, k);
          if (T === -1) L = m, x = E, R = k + 1, I = P.length;
          else {
            var S = T + 1;
            if (S >= P.length) L = m, x = E, R = k + 1, I = T;
            else if (P.charCodeAt(S) === U5) {
              var a = P.indexOf(")", S + 1);
              a === -1 ? (L = m, x = E, R = k + 1, I = T, k = P.length) : k = a;
            } else if (P.charCodeAt(S) === z0) {
              var g = P.indexOf("]", S + 1);
              g === -1 ? (L = m, x = E, R = k + 1, I = T, k = P.length) : k = g;
            } else k = T;
          }
        }
      }
      if (L >= 0) {
        var X0 = x ? "" : P.slice(R, I);
        P = P.slice(0, L) + X0, w = true;
      }
    }
    let o0 = P.match(/<([A-Z][A-Za-z0-9]*)(?:\s[^>]*)?>([^<]*)$/);
    if (o0 && o0.index !== void 0) {
      var n = o0[0].length - o0[2].length, y0 = n >= 2 && o0[0].charCodeAt(n - 2) === H0;
      if (!y0) {
        for (var C0 = false, m0 = 0, t = 0; t < o0.index; t++) P.charCodeAt(t) === e && m0++;
        if (C0 = m0 % 2 === 1, !C0) {
          let D0 = o0[1];
          f5(P, "</" + D0, 0) === -1 && (P = P.slice(0, o0.index) + o0[2]);
        }
      }
    }
    P !== F0 && ($ = $.slice(0, Q) + P, Z = Q + P.length);
  }
  let l = [];
  var j0 = [];
  let Z0 = Q;
  var Y0 = "", w0 = G.disableAutoLink || G.disableBareUrls || K.inAnchor ? -1 : $.indexOf("@", Q);
  for (w0 >= Z && (w0 = -1); Q < Z; ) {
    let P = $.charCodeAt(Q), F0 = null;
    if (P === e) {
      if (F0 = _6($, Q, Z), !F0) {
        var O0 = h5($, Q, Z, e);
        Q += O0 - 1;
      }
    } else if (P === A0 || P === q0) {
      var M0 = P6($, Q, Z);
      if (M0) {
        if (M0.canOpen || M0.canClose) {
          (Y0 || Q > Z0) && (l.push(i5(Y0 + $.slice(Z0, Q))), Y0 = "");
          var _0 = $.slice(Q, Q + M0.len), R0 = i5(_0);
          j0.push({ idx: l.length, ch: P, len: M0.len, canOpen: M0.canOpen, canClose: M0.canClose, active: true }), l.push(R0), Q += M0.len, Z0 = Q;
          continue;
        }
        Q += M0.len - 1;
      }
    } else if (P === N0) F0 = v6($, Q, Z, K, G);
    else if (P === V5) F0 = N6($, Q, Z, K, G);
    else if (P === z0) Q + 1 < Z && $.charCodeAt(Q + 1) === F5 && (F0 = b6($, Q, Z, K)), F0 || (F0 = z4($, Q, Z, K, G));
    else if (P === p0 && Q + 1 < Z && $.charCodeAt(Q + 1) === z0) F0 = z4($, Q, Z, K, G);
    else if (P === Q0) F0 = v4($, Q, Z), !F0 && !G.disableParsingRawHTML && !G.ignoreHTMLBlocks && (F0 = k6($, Q, Z, K, G));
    else if (P === F9) {
      var I0 = L6($, Q, Z);
      if (I0 !== -1) {
        var J0 = $.slice(Q, I0), h = o5(J0);
        if (h !== J0) {
          Y0 = Y0 + $.slice(Z0, Q) + h, Q = I0, Z0 = I0;
          continue;
        }
      }
    } else if ((P === q9 || P === S5 || P === P5) && !K.inAnchor && !G.disableAutoLink && (Q === 0 || $.charCodeAt(Q - 1) !== Q0)) {
      var B0 = Q + 1 < Z ? $.charCodeAt(Q + 1) : 0;
      (P === q9 && B0 === p5 || P === P5 && B0 === p5 || P === S5 && B0 === S5) && (F0 = y6($, Q, Z, G));
    }
    if (!F0 && w0 >= 0 && w0 - Q <= 64 && !K.inAnchor && !G.disableAutoLink && !G.disableBareUrls && (P >= p && P <= c || P >= G0 && P <= U0 || P >= r && P <= i) && (F0 = R6($, Q, Z, G), !F0 && Q >= w0 && (w0 = $.indexOf("@", Q + 1), w0 >= Z && (w0 = -1))), P === f && K.f) {
      var L0 = false, v0 = 0;
      if (Q > Z0 && $.charCodeAt(Q - 1) === b0) L0 = true, v0 = 1;
      else {
        for (var e0 = 0, O5 = Q - 1; O5 >= Z0 && $.charCodeAt(O5) === N; ) e0++, O5--;
        e0 >= 2 && (L0 = true, v0 = e0);
      }
      if (L0) {
        for ((Y0 || Q - v0 > Z0) && (l.push(i5(Y0 + $.slice(Z0, Q - v0))), Y0 = ""), l.push({ type: _.breakLine }), Q++; Q < Z && $.charCodeAt(Q) === N; ) Q++;
        Z0 = Q;
        continue;
      }
      var H5 = Q > Z0 && $.charCodeAt(Q - 1) === N, l0 = Q + 1 < Z && $.charCodeAt(Q + 1) === N;
      if (H5 || l0) {
        for (var C = Q; C > Z0 && $.charCodeAt(C - 1) === N; ) C--;
        for (Y0 += $.slice(Z0, C) + `
`, Q++; Q < Z && $.charCodeAt(Q) === N; ) Q++;
        Z0 = Q;
        continue;
      }
    }
    if (F0) (Y0 || Q > Z0) && (l.push(i5(Y0 + $.slice(Z0, Q))), Y0 = ""), l.push(F0.node), Q = F0.end, Z0 = Q;
    else {
      if (P === b0 && Q + 1 < Z) {
        let h0 = $.charCodeAt(Q + 1);
        if (X$(h0) & c5) {
          (Y0 || Q > Z0) && (l.push(i5(Y0 + $.slice(Z0, Q))), Y0 = ""), l.push(i5($[Q + 1])), Q += 2, Z0 = Q;
          continue;
        }
      }
      if (Q++, w0 < 0 || w0 - Q > 64) for (; Q < Z && !(w0 >= 0 && w0 - Q <= 64); ) {
        var $5 = $.charCodeAt(Q);
        if ($5 < t5 && !B4[$5]) Q++;
        else break;
      }
    }
  }
  return (Y0 || Z > Z0) && (l.push(i5(Y0 + $.slice(Z0, Z))), Y0 = ""), j0.length > 0 && w6(l, j0, Y, G), Y.h = V, O9--, l;
}
var E6 = 500;
function m5($, Q, Z) {
  var Y = Q.j || 0;
  if (Y > E6) return [{ type: _.text, text: $ }];
  Q.j = Y + 1;
  let G = Q;
  if (Z.optimizeForStreaming && !Q.e && !Q.n) {
    for (var V = $.length; V > 0 && $.charCodeAt(V - 1) === f; ) V--;
    for (var K = V; K > 0 && $.charCodeAt(K - 1) !== f; ) K--;
    if (V > K && $.charCodeAt(K) === S0) {
      for (var X = false, J = K + 1; J < V; J++) {
        var F = $.charCodeAt(J);
        if (F !== N && F !== b && F !== S0 && F !== g0 && F !== d) {
          X = true;
          break;
        }
      }
      for (var U = false, W = false, O = 0, z = K, B2 = K - 1; B2 > 0; ) {
        for (var A = B2; A > 0 && $.charCodeAt(A - 1) !== f; ) A--;
        if ($.charCodeAt(A) !== S0) break;
        z = A;
        for (var q = true, H = false, j2 = A; j2 < B2; j2++) {
          var M = $.charCodeAt(j2);
          if (M === d) H = true;
          else if (M !== N && M !== b && M !== g0 && M !== S0) {
            q = false;
            break;
          }
        }
        q && H ? (U = true, W = true) : W || O++, B2 = A - 1;
      }
      X ? U || ($ = $.slice(0, K).trimEnd()) : $ = U && O > 0 ? $.slice(0, K).trimEnd() : $.slice(0, z).trimEnd();
    }
    for (var y = -1, v = -1, D = -1, w = $.length - 1; w >= 0; w--) if ($.charCodeAt(w) === Q0) {
      var L = w + 1 < $.length ? $.charCodeAt(w + 1) : 0;
      if (L >= p && L <= c || L >= G0 && L <= U0) {
        for (var R = w + 2; R < $.length; ) {
          var I = $.charCodeAt(R);
          if (I >= p && I <= c || I >= G0 && I <= U0 || I >= r && I <= i) R++;
          else break;
        }
        for (var x = R; x < $.length && $.charCodeAt(x) !== o; ) x++;
        if (x < $.length && $.charCodeAt(x - 1) !== H0) {
          for (var k = false, u = x + 1; u < $.length; u++) if ($.charCodeAt(u) === Q0) {
            k = true;
            break;
          }
          k || (y = w, v = R, D = x + 1);
        }
      }
      break;
    }
    if (y >= 0) {
      for (var E = 0, m = 0; m < y; m++) $.charCodeAt(m) === e && E++;
      if (E % 2 === 0) {
        var T = $.slice(y + 1, v);
        f5($, "</" + T, 0) === -1 && ($ = $.slice(0, y) + $.slice(D));
      }
    }
    for (var S = $.length; S > 0 && $.charCodeAt(S - 1) === f; ) S--;
    if (S > 0) {
      for (var a = S; a > 0 && $.charCodeAt(a - 1) !== f; ) a--;
      for (var g = a, X0 = 0; g < S && $.charCodeAt(g) === N && X0 < 3; ) g++, X0++;
      if (a > 0 && g < S && W6($, g, S)) {
        for (var n = a - 1, y0 = n; y0 > 0 && $.charCodeAt(y0 - 1) !== f; ) y0--;
        a0($, y0, n) || ($ = $.slice(0, a).trimEnd());
      }
    }
    var C0 = $.length;
    if (C0 > 0) {
      for (var m0 = $.lastIndexOf(`
`), t = m0 === -1 ? 0 : m0 + 1, l = C0, j0 = t, Z0 = 0; j0 < l && $.charCodeAt(j0) === N && Z0 < 3; ) j0++, Z0++;
      if (j0 < l) {
        var Y0 = $.charCodeAt(j0), w0 = false;
        if (Y0 === A0 || Y0 === d || Y0 === b5) {
          var O0 = j0 + 1;
          if (O0 >= l || $.charCodeAt(O0) === N || $.charCodeAt(O0) === b) {
            for (var M0 = O0; M0 < l && ($.charCodeAt(M0) === N || $.charCodeAt(M0) === b); ) M0++;
            M0 >= l && (w0 = true);
          }
        } else if (Y0 >= r && Y0 <= i) {
          for (var _0 = j0; _0 < l && $.charCodeAt(_0) >= r && $.charCodeAt(_0) <= i; ) _0++;
          if (_0 < l && ($.charCodeAt(_0) === s0 || $.charCodeAt(_0) === q5)) {
            var R0 = _0 + 1;
            if (R0 >= l || $.charCodeAt(R0) === N || $.charCodeAt(R0) === b) {
              for (var I0 = R0; I0 < l && ($.charCodeAt(I0) === N || $.charCodeAt(I0) === b); ) I0++;
              I0 >= l && (w0 = true);
            }
          }
        }
        w0 && ($ = $.slice(0, t).trimEnd());
      }
    }
  }
  if (Q.inline) return _5($, 0, $.length, Q, Z);
  let J0 = [], h = 0, B0 = $.length;
  if (h === 0 && !Z.disableFrontmatter && $.startsWith("---")) {
    let C = g$($);
    if (C && C.hasValidYaml) {
      if (Z.preserveFrontmatter !== false) {
        let $5 = $.slice(0, C.endPos).trimEnd();
        J0.push({ type: _.frontmatter, text: $5 });
      }
      h = C.endPos;
    }
  }
  for (; h < B0; ) {
    for (var L0 = $.indexOf(`
`, h), v0 = L0 < 0 ? B0 : L0; h < B0 && a0($, h, v0); ) h = v0 < B0 ? v0 + 1 : v0, h < B0 && (L0 = $.indexOf(`
`, h), v0 = L0 < 0 ? B0 : L0);
    if (h >= B0) break;
    var e0 = $.charCodeAt(h) === L9;
    P0($, h, v0);
    let C = null;
    if (Q$ = $, Y$ = h, T9 = v0, !e0 && V0 >= 4 && !Q.inHTML) C = O6($, h);
    else if (!e0) {
      let $5 = h + $0, P = $.charCodeAt($5);
      P === G5 ? C = q6($, h, Q, Z) : P === o ? C = H6($, h, Q, Z) : P === e || P === N0 ? C = z6($, h, Q) : P === d || P === A0 || P === q0 ? (C = C5($, h), !C && (C = J4($, h, Q, Z))) : P === b5 || P >= r && P <= i ? C = J4($, h, Q, Z) : P === Q0 ? C = I4($, h, Q, Z) : P === S0 ? C = U4($, h, Q, Z) : P === z0 && (C = A6($, h, Q));
    }
    if (!C) {
      for (var O5 = false, H5 = h; H5 < v0; H5++) if ($.charCodeAt(H5) === S0) {
        O5 = true;
        break;
      }
      O5 && (C = U4($, h, Q, Z));
    }
    if (C || (C = M6($, h, Q, Z)), C) C.node.type !== _.refCollection && J0.push(C.node), h = C.end;
    else {
      var l0 = $.indexOf(`
`, h);
      h = l0 < 0 ? B0 : l0 + 1;
    }
  }
  return Q.j = Y, J0;
}
function N4($, Q) {
  $.charCodeAt(0) === 65279 && ($ = $.slice(1));
  let Z = { inline: false, inAnchor: false, inHTML: false, inList: false, inBlockQuote: false, refs: {} }, Y = Q == null ? void 0 : Q.slugify, G = { ...Q, slugify: Y ? (V) => Y(V, f0) : f0, sanitizer: (Q == null ? void 0 : Q.sanitizer) || j5, tagfilter: (Q == null ? void 0 : Q.tagfilter) !== false };
  return D9($, Z, G);
}
function P4($, Q, Z, Y) {
  var G = M5($, Q);
  return G ? { tagName: G.tag, tagLower: G.tag.toLowerCase(), attrs: G.rawAttrs, whitespaceBeforeAttrs: G.whitespaceBeforeAttrs, isSelfClosing: G.selfClosing, hasSpaceBeforeSlash: G.hasSpaceBeforeSlash, isClosing: G.isClosing, hasNewline: G.whitespaceBeforeAttrs.includes(`
`) || G.rawAttrs.includes(`
`), endPos: G.end } : null;
}
function D9($, Q, Z) {
  O9 = 0, $ = u$($), !Q.refs && (Q.refs = {}), (Z.optimizeForStreaming || Q.inline) && (Q.n = U6($, Q.refs, Z));
  var Y, G;
  Q.m || (Q.m = Y = [], Q.k = G = []);
  let V = m5($, Q, Z);
  if (Y && G) {
    for (var K = Y, X = { inline: Q.inline, inAnchor: Q.inAnchor, inHTML: Q.inHTML, htmlDepth: Q.c, inList: Q.inList, inBlockQuote: Q.inBlockQuote, noSetext: Q.g, depth: Q.j }, J = 0; J < K.length; J++) {
      var F = K[J];
      Q.inline = F.inline, Q.inAnchor = F.inAnchor, Q.inHTML = F.inHTML, Q.c = F.htmlDepth, Q.inList = F.inList, Q.inBlockQuote = F.inBlockQuote, Q.g = F.noSetext, Q.j = F.depth, Q.f = F.breaks;
      var U = _5(F.text, 0, F.text.length, Q, Z);
      Q.f = false;
      for (var W = 0; W < U.length; W++) F.dest.push(U[W]);
    }
    Q.inline = X.inline, Q.inAnchor = X.inAnchor, Q.inHTML = X.inHTML, Q.c = X.htmlDepth, Q.inList = X.inList, Q.inBlockQuote = X.inBlockQuote, Q.g = X.noSetext, Q.j = X.depth;
    for (var O = G, z = 0; z < O.length; z++) for (var B2 = O[z], A = 0; A < B2.src.length; A++) {
      var q = B2.src[A];
      if (B2.unwrap && q.type === _.paragraph) for (var H = q.children, j2 = 0; j2 < H.length; j2++) B2.dest.push(H[j2]);
      else B2.dest.push(q);
    }
    Q.m = void 0, Q.k = void 0;
  }
  return S9(Q.refs) ? [{ type: _.refCollection, refs: Q.refs }, ...V] : V;
}
var F$;
try {
  a5 = c0.createElement("div"), F$ = a5 !== null && typeof a5 == "object" && "$$typeof" in a5 && typeof a5.$$typeof == "symbol" ? a5.$$typeof : Symbol.for("react.transitional.element");
} catch {
  F$ = Symbol.for("react.transitional.element");
}
var a5;
function U$($, Q, Z) {
  return { $$typeof: F$, type: $, key: Z != null ? "" + Z : null, ref: null, props: Q, _owner: null, _store: {}, _debugStack: null, _debugTask: null };
}
function z5($, Q) {
  var Z = { key: $ };
  if (Q) {
    var Y = i9(Q);
    for (var G in Y) Z[G] = Y[G];
  }
  return Z;
}
var f9 = typeof c0.createContext < "u" ? c0.createContext(void 0) : void 0;
function w4($, Q, Z, Y, G, V, K, X, J) {
  switch ($.type) {
    case _.blockQuote: {
      let q = { key: Z.key };
      return $.alert && (q.className = "markdown-alert-" + V($.alert.toLowerCase(), f0), $.children.unshift(p$($.alert))), Y("blockquote", q, Q($.children, Z));
    }
    case _.breakLine:
      return Y("br", { key: Z.key });
    case _.breakThematic:
      return Y("hr", { key: Z.key });
    case _.frontmatter:
      return X.preserveFrontmatter ? Y("pre", { key: Z.key }, $.text) : null;
    case _.codeBlock:
      let A = $.lang ? o5($.lang) : "";
      var F = $.attrs ? i9($.attrs) : {};
      return F.className = A ? "language-" + A + " lang-" + A : "", Y("pre", { key: Z.key }, Y("code", F, $.text));
    case _.codeInline:
      return Y("code", { key: Z.key }, $.text);
    case _.footnoteReference:
      return Y("a", { key: Z.key, href: G($.target, "a", "href") || void 0 }, Y("sup", null, $.text));
    case _.gfmTask:
      return Y("input", { checked: $.completed, key: Z.key, readOnly: true, type: "checkbox" });
    case _.heading:
      return Y(`h${$.level}`, { id: $.id, key: Z.key }, Q($.children, Z));
    case _.htmlBlock: {
      let q = $;
      if (X.tagfilter && s9(q.tag)) return Y("span", { key: Z.key }, "<" + q.tag + n9(q.attrs) + ">");
      if (q.a && q.d) {
        let H = q.tag.toLowerCase(), j2 = D4(H), M = q.children, y = M != null && M.length > 0;
        if (j2) {
          let T = l$(q.a, H, X.tagfilter);
          if (/<[a-z][^>]{0,100}>/i.test(q.a)) {
            var U = z5(Z.key, $.attrs);
            return U.dangerouslySetInnerHTML = { __html: T }, J($.tag, U);
          }
          return J($.tag, z5(Z.key, $.attrs), T);
        }
        let v = new RegExp(`^<${q.tag}(\\s|>)`, "i");
        if (y && !v.test(q.a) && X.tagfilter && a$(q.a)) return J($.tag, z5(Z.key, $.attrs), Q(M, Z));
        if (A4(q.a)) {
          let T = X.tagfilter ? t9(q.a) : q.a;
          var W = z5(Z.key, $.attrs);
          return W.dangerouslySetInnerHTML = { __html: T }, J($.tag, W);
        }
        let w = { slugify: (T) => V(T, f0), sanitizer: G, tagfilter: true }, L = q.a.replace(/>\s+</g, "><").replace(/\n+/g, " ").trim();
        if (new RegExp(`^<${q.tag}(\\s[^>]*)?>(\\s*</${q.tag}>)?$`, "i").test(L)) return q.children && q.children.length > 0 ? J($.tag, z5(Z.key, $.attrs), Q(q.children, Z)) : J($.tag, z5(Z.key, $.attrs));
        let I = D9(L, { inline: false, refs: K, inHTML: false }, w);
        a9(I);
        let x = q.tag.toLowerCase(), k = "</" + x + ">", u = v.test(L), E = L.toLowerCase().trimEnd().endsWith(k), m = u && E;
        if (m && y) return J($.tag, z5(Z.key, $.attrs), Q(M, Z));
        if (m) return Q(I.flatMap(l5), Z);
        var O = c$(I, x);
        if (O.found && O.afterClose.length > 0) {
          var z = O.beforeClose.flatMap(l5), B2 = O.afterClose.flatMap(l5);
          return U$(c0.Fragment, { children: [J($.tag, z5(Z.key, $.attrs), Q(z, Z)), Q(B2, Z)] }, Z.key);
        }
        return J($.tag, z5(Z.key, $.attrs), Q(I.flatMap(l5), Z));
      }
      return r5($.tag) ? J($.tag, z5(Z.key, $.attrs)) : J($.tag, z5(Z.key, $.attrs), $.children ? Q($.children, Z) : "");
    }
    case _.htmlSelfClosing: {
      let q = $;
      return X.tagfilter && s9(q.tag) ? Y("span", { key: Z.key }, "<" + q.tag + n9(q.attrs) + " />") : J($.tag, z5(Z.key, $.attrs));
    }
    case _.image: {
      let q = $.target != null ? G($.target, "img", "src") : null;
      return Y("img", { key: Z.key, alt: $.alt && $.alt.length > 0 ? $.alt : void 0, title: $.title || void 0, src: q || void 0 });
    }
    case _.link: {
      let q = { key: Z.key };
      return $.target != null && (q.href = r$($.target)), $.title && (q.title = $.title), Y("a", q, Q($.children, Z));
    }
    case _.table: {
      let q = $;
      return Y("table", { key: Z.key }, Y("thead", { key: "thead" }, Y("tr", null, q.header.map(function(j2, M) {
        return Y("th", { key: M, style: q.align[M] == null ? {} : { textAlign: q.align[M] } }, Q(j2, Z));
      }))), q.cells.length > 0 && Y("tbody", { key: "tbody" }, q.cells.map(function(j2, M) {
        return Y("tr", { key: M }, j2.map(function(v, D) {
          return Y("td", { key: D, style: q.align[D] == null ? {} : { textAlign: q.align[D] } }, Q(v, Z));
        }));
      })));
    }
    case _.text:
      return $.text;
    case _.textFormatted:
      return Y($.tag, { key: Z.key }, Q($.children, Z));
    case _.orderedList:
    case _.unorderedList: {
      let q = $.type === _.orderedList ? "ol" : "ul";
      return Y(q, { key: Z.key, start: $.type === _.orderedList ? $.start : void 0 }, $.items.map(function(j2, M) {
        return Y("li", { key: M }, Q(j2, Z));
      }));
    }
    case _.paragraph:
      return Y("p", { key: Z.key }, Q($.children, Z));
    case _.ref:
      return null;
    default:
      return null;
  }
}
var x6 = ($, Q, Z, Y, G, V, K) => {
  var X = (F) => F.map(function(U) {
    return "text" in U ? U.text : "";
  }), J = (F, U = {}) => {
    var W = Array.isArray(F) ? F : [F], O = (U.renderDepth || 0) + 1;
    if (O > 2500) return X(W);
    U.renderDepth = O;
    for (var z = U.key, B2 = [], A = false, q = 0; q < W.length; q++) {
      U.key = q;
      var H;
      if ($) {
        var j2 = w4.bind(null, W[q], J, U, Q, Z, Y, G, V, K);
        H = $(j2, W[q], J, U);
      } else H = w4(W[q], J, U, Q, Z, Y, G, V, K);
      var M = typeof H == "string";
      if (A && typeof H == "string") {
        var y = B2[B2.length - 1];
        B2[B2.length - 1] = (typeof y == "string" ? y : "") + H;
      } else if (H !== null) if (Array.isArray(H)) for (var v = 0; v < H.length; v++) B2.push(H[v]);
      else B2.push(H);
      A = M;
    }
    return U.key = z, U.renderDepth = O - 1, B2;
  };
  return J;
};
var g6 = ($, Q) => {
  let Z = e5(Q, $, void 0);
  return Z ? typeof Z == "function" || typeof Z == "object" && "render" in Z ? Z : e5(Q, `${$}.component`, $) : $;
};
function f6($, Q) {
  let Z = { ...Q || {} };
  Z.overrides = Z.overrides || {};
  let Y = Z.slugify || f0, G = Z.sanitizer || j5, V = Z.createElement, K = S9(Z.overrides), X = (y) => A9(y, { ...Z, wrapper: null });
  function J(y) {
    for (var v in y) {
      var D = y[v];
      if (typeof D == "string" && D.length > 0 && D.charCodeAt(0) === Q0 && (O4.test(D) || H4.test(D) || P4(D, 0))) {
        var w = X(D.trim());
        y[v] = v === "innerHTML" && Array.isArray(w) ? w[0] : w;
      }
    }
  }
  function F(y, v, ...D) {
    var w = v || {}, L = y;
    if (K) {
      var R = e5(Z.overrides, y + ".props", {});
      L = g6(y, Z.overrides), w = { ...w, ...R, className: i$(w.className, R.className) || void 0 };
    }
    if (!V) {
      var I = w.key;
      return I != null && delete w.key, D.length === 1 ? w.children = D[0] : D.length > 1 && (w.children = D), U$(L, w, I);
    }
    return V(L, w, ...D);
  }
  function U(y, v, ...D) {
    return v && J(v), F(y, v, ...D);
  }
  let W = Z.slugify, O = { ...Z, slugify: W ? (y) => W(y, f0) : f0, sanitizer: G, tagfilter: Z.tagfilter !== false }, z = $[0] && $[0].type === _.refCollection ? $[0].refs : {}, B2 = x6(Z.renderRule, F, G, Y, z, Z, U), A = B2($, { inline: Z.forceInline, refs: z }), q = o$(z);
  if (q.length && A.push(F("footer", { key: "footer" }, q.map(function(v) {
    let D = v.identifier.charCodeAt(0) === F5 ? v.identifier.slice(1) : v.identifier, w = D9(v.footnote, { inline: true, refs: z }, O);
    return F("div", { id: Y(D, f0), key: v.identifier }, D + ": ", B2(w, { inline: true, refs: z }));
  }))), Z.wrapper === null) return A;
  let H = Z.wrapper || (Z.forceInline ? "span" : "div"), j2;
  if (A.length > 1 || Z.forceWrapper) j2 = A;
  else return A.length === 1 ? A[0] : null;
  var M = Z.wrapperProps ? { ...Z.wrapperProps } : {};
  return M.children = j2, U$(H, M, "outer");
}
function A9($ = "", Q = {}) {
  let Z = { ...Q || {} };
  Z.overrides = Z.overrides || {};
  let Y = Z.slugify || f0, G = Z.sanitizer || j5;
  function V(X) {
    let J = Z.forceInline || !Z.forceBlock && !f$.test(X), F = Z.slugify, U = { ...Z, slugify: F ? (B2) => F(B2, f0) : f0, sanitizer: G, tagfilter: Z.tagfilter !== false }, W = J ? X : d$(X);
    if (Z.optimizeForStreaming) {
      var O = W.lastIndexOf("<");
      O !== -1 && W.indexOf(">", O) === -1 && (W = W.slice(0, O));
    }
    let z = D9(W, { inline: J, refs: K }, U);
    return f6(z, { ...U, forceInline: J });
  }
  let K = {};
  return V($);
}
function C6($) {
  let Q = c0.useRef($), Z = Q.current;
  if (Z !== $) {
    var Y = true, G = 0;
    for (var V in $) if (G++, !Object.is(Z[V], $[V])) {
      Y = false;
      break;
    }
    if (Y) {
      var K = 0;
      for (var X in Z) K++;
      Y = K === G;
    }
    Y || (Q.current = $);
  }
  return Q.current;
}
var q$ = ({ children: $, options: Q, ...Z }) => {
  if (!(typeof c0.useContext < "u")) {
    let F = { ...Q, overrides: { ...Q == null ? void 0 : Q.overrides }, wrapperProps: { ...Q == null ? void 0 : Q.wrapperProps, ...Z } };
    return A9($ ?? "", F);
  }
  let G = f9 ? c0.useContext(f9) : void 0, V = C6(Z), K = c0.useMemo(() => ({ ...G, ...Q, overrides: { ...G == null ? void 0 : G.overrides, ...Q == null ? void 0 : Q.overrides }, wrapperProps: { ...G == null ? void 0 : G.wrapperProps, ...Q == null ? void 0 : Q.wrapperProps, ...V } }), [G, Q, V]), X = $ ?? "";
  return c0.useMemo(() => A9(X, K), [X, K]);
};
var b4 = q$;
var XZ = q$;
var JZ = A9;
var zZ = b4;
export {
  XZ as Markdown,
  _ as RuleType,
  JZ as compiler,
  zZ as default,
  N4 as parser,
  j5 as sanitizer,
  f0 as slugify
};
//# sourceMappingURL=markdown-to-jsx.js.map
